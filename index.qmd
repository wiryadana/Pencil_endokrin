---
title: "Hubungan Faktor Risiko Klinis dan Indeks Kardiometabolik terhadap Kerentanan Plak dan Beban Kalsifikasi Koroner pada Pasien Diabetes Mellitus Tipe 2"
date: now
date-format: "DD MMMM YYYY, HH:mm"
author:
  - name: Kadek Adit Wiryadana; Made Ratna Saraswati
    email: wiryadana.2371041012@student.unud.ac.id
    corresponding: true
    affiliations:
      - name: RSUP Ngoerah
        city: Denpasar
        country: Indonesia
abstract: |
  **Latar Belakang:** Penyakit kardiovaskular adalah komplikasi utama *Diabetes Mellitus* (DM). Penelitian ini bertujuan mengevaluasi asosiasi faktor risiko konvensional dan indeks kardiometabolik turunan dengan kerentanan *High-Risk Plaque* (HRP) dan keparahan kalsifikasi arteri koroner (CAC) pada pasien DM.
  
  **Metode:** Studi potong lintang mengevaluasi pasien DM yang menjalani *Computed Tomography Coronary Angiography* (CTCA) di RSUP Ngoerah pada tahun 2025. Profil klinis (usia, jenis kelamin, pengobatan, kreatinin, profil lipid, HbA1c) dan indeks kardiometabolik turunan (Indeks TyHbA1c, Rasio TG/HDL, Kolesterol Non-HDL, Rasio SUA/HDL) dianalisis menggunakan komparasi bivariat untuk status HRP dan korelasi linear untuk skor CAC.
  
  **Hasil:** Sampel penelitian berjumlah 52 pasien, 30,8% merupakan pasien baru terdiagnosis DM. Pada kasus DM lama (69,2%), terapi yang diterima meliputi antidiabetik oral (32,7%), regimen insulin (32,7%), dan tanpa pengobatan (3,8%). Sebagian besar pasien (71,2%) menerima terapi statin dan dirujuk karena riwayat kejadian kardiovaskular sebelumnya. HRP teridentifikasi pada 71,2% pasien. Kelompok HRP-positif secara signifikan lebih tua (63,16 vs 55,8 tahun, p<0,01) dengan beban CAC yang lebih tinggi (median 556,7 vs 11,6 Agatston, p<0,001). Indeks kardiometabolik turunan tidak menunjukkan diskriminasi yang bermakna terhadap HRP. Sebaliknya, analisis korelasi mengungkapkan asosiasi negatif yang paradoksikal antara indeks turunan lipid dan keparahan CAC. Evaluasi stratifikasi mengindikasikan bahwa anomali ini terutama disetir oleh durasi DM yang panjang dan terapi statin.
  
  **Simpulan:** Usia merupakan determinan utama aterosklerosis koroner pada pasien DM. Hubungan terbalik yang paradoksikal antara indeks kardiometabolik dengan CAC menyoroti efek perancu dari terapi statin, yang menjadikan kadar lipid tampak optimal secara semu pada kasus yang parah sehingga membatasi nilai prediktif klinis mandirinya.

  ---

  **Abstract**

  **Background:** Cardiovascular disease is a major complication of Diabetes Mellitus (DM). This study aims to evaluate the association of conventional risk factors and derivative cardiometabolic indices with High-Risk Plaque (HRP) vulnerability and coronary artery calcification (CAC) severity in DM patients.
  
  **Methods:** A cross-sectional study evaluated DM patients undergoing Computed Tomography Coronary Angiography (CTCA) at RSUP Ngoerah in 2025. Clinical profiles (age, gender, medication, creatinine, lipid profile, HbA1c) and derivative cardiometabolic indices (TyHbA1c Index, TG/HDL Ratio, Non-HDL Cholesterol, SUA/HDL Ratio) were analyzed using bivariate comparisons for HRP status and linear correlations for CAC scores.
  
  **Results:** Among 52 patients, 30.8% were newly diagnosed DM. For known DM cases (69.2%), treatments included oral antidiabetics (32.7%), insulin regimens (32.7%), and no medication (3.8%). Most patients (71.2%) were receiving statin therapy and were referred due to a prior history of cardiac events. HRP was identified in 71.2% of patients. The HRP-positive group was significantly older (63.16 vs. 55.8 years, p<0.01) with higher CAC burdens (median 556.7 vs. 11.6 Agatston, p<0.001). Derivative cardiometabolic indices showed no significant discrimination for HRP. Conversely, correlation analysis revealed a paradoxical negative association between lipid-derived indices and CAC severity. Stratified evaluations indicated this anomaly was primarily driven by prolonged DM duration and statin therapy (statin paradox).
  
  **Conclusion:** Age is the primary determinant of coronary atherosclerosis in DM. The paradoxical inverse relationship of cardiometabolic indices with CAC highlights confounding effects from statin therapy, rendering lipid levels deceptively optimal in severe cases and limiting their standalone predictive clinical value.
keywords:
  - Cardiometabolic Index
  - Coronary Artery Calcification
  - Diabetes Mellitus
  - High-Risk Plaque
  - Statin Paradox
format:
  agu-pdf:
    keep-tex: true       
  html:
    embed-resources: true
    theme:
      light: flatly
      dark: darkly
    toc: true            
    number-sections: true
    code-fold: true
    warning: false
    message: false
  docx: 
    echo: false
    warning: false
    message: false
    reference-doc: template_pencil.docx
bibliography: pencil_endokrin.bib
csl: wiryadana_vancouver_style.csl
lang: id
crossref:
  tbl-title: Tabel        
  tbl-prefix: Tabel       
  fig-title: Gambar       
  fig-prefix: Gambar      
  title-delim: "."        
---

## Pendahuluan

Penyakit kardiovaskular aterosklerotik (ASCVD) tetap menjadi penyebab utama morbiditas dan mortalitas pada pasien dengan Diabetes Mellitus (DM). Patogenesis aterosklerosis pada populasi DM kompleks dan multifaktorial. Resistensi insulin kronis dan glukolipotoksisitas diakui sebagai faktor utama pemicu disfungsi endotel.[@bornfeldt2011insulin] Hal ini memicu ketertarikan terhadap berbagai indeks kardiometabolik turunan seperti Indeks TyHbA1c, Rasio Trigliserida terhadap HDL (TG/HDL), Kolesterol Non-HDL, hingga Rasio Asam Urat terhadap HDL (SUA/HDL) sebagai penanda biologis alternatif yang dihipotesiskan mampu merepresentasikan beban patofisiologis secara lebih presisi dibandingkan parameter lipid konvensional. [@jin2020triglyceride; @won2018relationship]

Di sisi lain, proses aterosklerosis tidak hanya dikendalikan oleh fluktuasi faktor metabolik, melainkan juga sangat bergantung pada faktor degeneratif (usia) dan durasi paparan stresor terhadap dinding vaskular.[@visseren2021esc] Lebih lanjut, literatur menegaskan bahwa komplikasi fungsi ginjal (Penyakit Ginjal Kronis/PGK) seringkali bertindak sebagai katalisator yang mempercepat progresi kalsifikasi. Kehadiran PGK mampu meningkatkan kerentanan jaringan vaskular melampaui efek parameter lipid atau glikemik saja.[@jankowski2021cardiovascular]

Seiring perkembangan alat diagnostik modern, evaluasi anatomi koroner non-invasif menggunakan *Computed Tomography Coronary Angiography* (CTCA) memungkinkan identifikasi *High-Risk Plaque* (HRP) sebagai prekursor sindrom koroner akut, sekaligus mengkuantifikasi beban kalsifikasi arteri koroner dengan *coronary artery calcification score* (*CAC Score*).[@motoyama2015plaque; @cury2022cadrads] Kendati berbagai penelitian indeks kardiometabolik turunan terus bermunculan, keunggulan prediktif aktualnya bila disandingkan langsung (*head-to-head*) dengan faktor risiko tradisional pada konteks praktik klinik sehari-hari masih belum konklusif. Oleh karena itu, penelitian ini bertujuan untuk mengeksplorasi hubungan faktor risiko tradisional dan indeks kardiometabolik turunan dengan adanya HRP dan beban kalsifikasi koroner pada kohort DM untuk memfasilitasi stratifikasi risiko yang lebih rasional pada praktek klinis.

## Metode

**Desain dan Populasi Penelitian**

Penelitian ini merupakan studi analitik observasional dengan pendekatan potong lintang (*cross-sectional*). Populasi target adalah pasien DM yang menjalani pemeriksaan CTCA di RSUP Prof. dr. I.G.N.G. Ngoerah pada periode 1 Januari 2025 hingga 31 Desember 2025. Teknik pengambilan sampel menggunakan *consecutive sampling*. Kriteria inklusi meliputi: (1) Usia $\ge$ 18 tahun; (2) Data rekam medis lengkap (usia, jenis kelamin, antropometri, durasi DM, terapi); (3) Hasil laboratorium lengkap dalam kurun 30 hari sebelum CTCA (HbA1c, profil lipid, asam urat, serum kreatinin). Pasien dengan riwayat *Coronary Artery Bypass Grafting* (CABG) atau *End-Stage Renal Disease* dieksklusi.

**Pengumpulan Data dan Definisi Operasional**

Data diekstraksi dari Sistem Informasi Manajemen Rumah Sakit (SIMRS). Guna memfasilitasi analisis komparatif klinis, variabel kontinu dikategorikan berdasarkan nilai pemisah (*cut-off*) pedoman standar. Usia dikelompokkan menjadi \< 60 tahun dan $\ge$ 60 tahun (lansia).[@kemenkes2022profil] Indeks Massa Tubuh (IMT) diklasifikasikan berdasarkan kriteria Asia-Pasifik menjadi normal/*overweight* (\< 25 kg/m$^2$) dan obesitas ($\ge$ 25 kg/m$^2$).[@who2004appropriate] Status hipertensi diklasifikasikan menjadi tiga kategori: tidak ada riwayat, terkontrol, dan tidak terkontrol (mencakup stadium I dan II) merujuk pada pedoman klinis *American College of Cardiology/American Heart Association* (ACC/AHA). [@whelton2018acc]

Pada parameter laboratorium dikategorikan dikotom, target glikemik (HbA1c) dikelompokkan menjadi terkontrol (\< 7,0%) dan tidak terkontrol ($\ge$ 7,0%).[@ada2024glycemic] Profil lipid dikategorikan menurut kriteria NCEP ATP III: Kolesterol Total tinggi ($\ge$ 200 mg/dL), Trigliserida tinggi ($\ge$ 150 mg/dL), LDL tidak optimal ($\ge$ 100 mg/dL), serta HDL rendah (\< 40 mg/dL pada laki-laki dan \< 50 mg/dL pada perempuan).[@ncep2001atp3] Hiperurisemia didefinisikan sebagai kadar asam urat serum \> 7,0 mg/dL pada laki-laki dan \> 6,0 mg/dL pada perempuan,[@bardin2014definition] sedangkan anemia jika hemoglobin \< 13,0 g/dL (laki-laki) dan \< 12,0 g/dL (perempuan).[@who2011haemoglobin]

Terdapat empat indeks kardiometabolik turunan yang dihitung secara spesifik: 1. **Indeks TyHbA1c** = $\ln(\text{Trigliserida [mg/dL]} \times \text{HbA1c [\%]})$;[@jin2020triglyceride] 2. **Rasio TG/HDL** = $\text{Trigliserida} / \text{HDL}$;[@mclaughlin2003is] 3. **Kolesterol Non-HDL** = $\text{Kolesterol Total} - \text{HDL}$;[@ncep2001atp3] dan 4. **Rasio SUA/HDL** = $\text{Asam Urat} / \text{HDL}$.[@kocak2019serum] Pemotongan (*cut-off*) diagnostik untuk Kolesterol Non-HDL ditetapkan pada batas $\ge$ 130 mg/dL sesuai pedoman NCEP ATP III [@ncep2001atp3], dan Rasio TG/HDL ditetapkan pada $\ge$ 3,0 sebagai penanda resistensi insulin [@mclaughlin2003is]. Mengingat ketiadaan konsensus baku untuk Indeks TyHbA1c dan Rasio SUA/HDL, kedua variabel novel ini dikotomisasi berdasarkan nilai median kohort penelitian (rendah dan tinggi) untuk keperluan komparasi analitik.Status fungsi ginjal dihitung menggunakan kriteria CKD-EPI, di mana PGK didefinisikan sebagai Stadium $\ge$ 3 (eGFR \< 60).[@levey2009new] Luaran dependen utama pada studi ini meliputi: **Keberadaan High-Risk Plaque (HRP):** Positif bila pada CTCA ditemukan fitur *positive remodeling*, *low-attenuation plaque*, *spotty calcification*, atau *napkin-ring sign*.[@motoyama2015plaque] Luaran kedua adalah **Beban Kalsifikasi Koroner (Skor CAC)** Dikuantifikasi menggunakan Skor Agatston absolut sesuai laporan interpretasi dokter spesialis radiologi. [@agatston1990quantification; @cury2022cadrads]

**Analisis Statistik**

Analisis dilakukan menggunakan perangkat lunak R.[@rcoreteam2026] Normalitas distribusi data numerik dievaluasi terlebih dahulu menggunakan uji Shapiro-Wilk. Data numerik dirangkum dalam rerata $\pm$ standar deviasi (bila berdistribusi normal) atau median (Q1-Q3) (bila tidak berdistribusi normal). Berdasarkan hasil tersebut, analisis komparasi bivariat untuk data numerik dilakukan menggunakan *Independent T-test* apabila data berdistribusi normal, sedangkan uji *Mann-Whitney U* digunakan sebagai alternatif non-parametrik apabila distribusi data tidak normal. Untuk data kategorik, komparasi dilakukan dengan uji *Chi-Square*, atau *Fisher’s Exact test* jika asumsi *Chi-Square* tidak terpenuhi. Analisis korelasi (uji Pearson untuk data berdistribusi normal atau Spearman rank untuk data tidak normal) dan grafik *scatter plot* digunakan untuk mengevaluasi hubungan antara dua variabel numerik. Pemodelan regresi logistik multivariat dilakukan dengan menyeleksi variabel yang bermakna secara statistik pada uji bivariat guna mencegah *overfitting*.

```{r setup}
#| message: false
#| warning: false

library(readxl)        # Untuk membaca data dari Excel lokal
library(dplyr)         # Untuk manipulasi data
library(pROC)          # Untuk analisis kurva ROC dan AUC
library(rms)           # Untuk regresi logistik dan validasi bootstrap
library(ggplot2)       # Untuk visualisasi data
library(gtsummary)     # Untuk membuat tabel hasil klinis yang rapi
library(tidyverse)
library(googlesheets4)
library(flextable)
library(haven)
library(tibble)
library(patchwork)
```

```{r load_data}
#| message: false
#| warning: false
#| paged-print: false

# Otorisasi Google Sheets (Pilih '0' atau login sesuai petunjuk di console R)
#gs4_auth(email = TRUE) 

gs4_deauth()

# Tautan Google Sheets
sheet_url <- "[https://docs.google.com/spreadsheets/d/18yyL48LTB-njD6iGNwiuYiiG4uCG1edVn6d3TYQnpE8/edit?gid=288598852#gid=288598852](https://docs.google.com/spreadsheets/d/18yyL48LTB-njD6iGNwiuYiiG4uCG1edVn6d3TYQnpE8/edit?gid=288598852#gid=288598852)"

# Membaca data
df_raw <- read_sheet(sheet_url, sheet = 1)

df_clean <- df_raw %>%
  filter(!is.na(`No. RM`)) %>%
  mutate(across(c(Usia, IMT, HbA1C, Trigliserida, Kreatinin, `Kolesterol-Total`, LDL, HDL, `Asam-Urat`, Hemoglobin), 
                ~ as.numeric(gsub(",", ".", as.character(.x))))) %>%
  mutate(
    Global_CAC_score = as.numeric(gsub(",", ".", gsub("[^0-9,.-]", "", as.character(Global_CAC_score)))),
    HRP_Status = ifelse(grepl("HRP", `Global_CAD-RADS`, ignore.case = TRUE), "HRP (+)", "HRP (-)"),
    HRP_Status = factor(HRP_Status, levels = c("HRP (-)", "HRP (+)")),
    CAC_100_plus = ifelse(Global_CAC_score >= 100, 1, 0),
    
    TyHbA1c_Index = log(Trigliserida * HbA1C),
    TG_HDL_Ratio = Trigliserida / HDL,
    Non_HDL = `Kolesterol-Total` - HDL,
    SUA_HDL_Ratio = `Asam-Urat` / HDL,
    
    Terapi_DM = factor(`OAD-INSULIN`, levels = c("Tanpa-obat", "OAD", "Insulin"), labels = c("Tanpa Terapi/Diet", "OAD", "Insulin")),
    Durasi_DM_Bulan = as.numeric(Durasi_DM),
    Riwayat_DM = ifelse(is.na(Durasi_DM_Bulan), NA, ifelse(Durasi_DM_Bulan == 0, "Baru Terdiagnosis", "Sudah Lama")),
    
    # Pengelompokan Hipertensi 
    Hipertensi_Kat = case_when(
      grepl("st I|st II|stI|stII|tidak terkontrol", Hipertensi, ignore.case = TRUE) ~ "Hipertensi Tidak Terkontrol",
      grepl("terkontrol", Hipertensi, ignore.case = TRUE) ~ "Hipertensi Terkontrol",
      TRUE ~ "Tidak ada riwayat hipertensi" # <--- Perhatikan huruf kecilnya
    ),
    # Ubah menjadi factor agar urutannya rapi di tabel
    Hipertensi_Kat = factor(Hipertensi_Kat, levels = c("Tidak ada riwayat hipertensi", "Hipertensi Terkontrol", "Hipertensi Tidak Terkontrol")), # <--- Harus SAMA PERSIS dengan di atas
    
    Kappa = ifelse(Gender == "P", 0.7, 0.9),
    Alpha = ifelse(Gender == "P", -0.241, -0.302),
    Sex_Mult = ifelse(Gender == "P", 1.012, 1),
    eGFR = 142 * (pmin(Kreatinin / Kappa, 1)^Alpha) * (pmax(Kreatinin / Kappa, 1)^-1.200) * (0.9938^Usia) * Sex_Mult,
    CKD_Status = ifelse(eGFR < 60, 1, ifelse(eGFR >= 60, 0, NA))
  ) %>%
  filter(!is.na(Usia), !is.na(CKD_Status), !is.na(HRP_Status), !is.na(Global_CAC_score))

n_total <- nrow(df_clean)
pct_hrp_pos <- round((sum(df_clean$HRP_Status == "HRP (+)") / n_total) * 100, 1)
```

## HASIL

```{r kalkulasi-utama}
#| include: false
#| warning: false
#| message: false

# 1. Total & HRP
n_total <- nrow(df_clean)
n_hrp_pos <- sum(df_clean$HRP_Status == "HRP (+)", na.rm = TRUE)
pct_hrp_pos <- format(round((n_hrp_pos / n_total) * 100, 1), decimal.mark=",")

# 2. Usia HRP Positif vs Negatif
mean_usia_pos <- format(round(mean(df_clean$Usia[df_clean$HRP_Status == "HRP (+)"], na.rm = TRUE), 1), decimal.mark=",")
mean_usia_neg <- format(round(mean(df_clean$Usia[df_clean$HRP_Status == "HRP (-)"], na.rm = TRUE), 1), decimal.mark=",")

# 3. Kalsifikasi Absolut (Agatston)
med_cac_pos <- format(round(median(df_clean$Global_CAC_score[df_clean$HRP_Status == "HRP (+)"], na.rm = TRUE), 1), decimal.mark=",")
med_cac_neg <- format(round(median(df_clean$Global_CAC_score[df_clean$HRP_Status == "HRP (-)"], na.rm = TRUE), 1), decimal.mark=",")

# 4. Profil Terapi dan Durasi DM
n_dm_baru <- sum(df_clean$Riwayat_DM == "Baru Terdiagnosis", na.rm = TRUE)
pct_dm_baru <- format(round((n_dm_baru / n_total) * 100, 1), decimal.mark=",")
n_oad <- sum(df_clean$Terapi_DM == "OAD", na.rm = TRUE)
pct_oad <- format(round((n_oad / n_total) * 100, 1), decimal.mark=",")
n_ins <- sum(df_clean$Terapi_DM == "Insulin", na.rm = TRUE)
pct_ins <- format(round((n_ins / n_total) * 100, 1), decimal.mark=",")
n_diet <- sum(df_clean$Terapi_DM == "Tanpa Terapi/Diet", na.rm = TRUE)
pct_diet <- format(round((n_diet / n_total) * 100, 1), decimal.mark=",")

n_statin <- sum(df_clean$Statin == "On-Statin", na.rm = TRUE)
pct_statin <- format(round((n_statin / n_total) * 100, 1), decimal.mark=",")

# 5. Paradoks CAC pada Pengguna Statin
med_cac_statin_pos <- format(round(median(df_clean$Global_CAC_score[df_clean$Statin == "On-Statin"], na.rm = TRUE), 1), decimal.mark=",")
med_cac_statin_neg <- format(round(median(df_clean$Global_CAC_score[df_clean$Statin == "No-Statin"], na.rm = TRUE), 1), decimal.mark=",")

# 6. Bukti Tabel 4 (Anomali Kolesterol Non-HDL)
n_nhdl_opt <- sum(df_clean$Non_HDL < 130, na.rm = TRUE)
n_nhdl_opt_lama <- sum(df_clean$Non_HDL < 130 & df_clean$Riwayat_DM == "Sudah Lama", na.rm = TRUE)
pct_lama_opt <- format(round((n_nhdl_opt_lama / n_nhdl_opt) * 100, 0), decimal.mark=",")

# 7. Bukti Tabel 5 (Validasi Rasio TG/HDL)
n_tg_norm <- sum(df_clean$TG_HDL_Ratio < 3.0, na.rm = TRUE)
n_tg_norm_terkontrol <- sum(df_clean$TG_HDL_Ratio < 3.0 & df_clean$HbA1C < 7.0, na.rm = TRUE)
pct_hba1c_tg_norm <- format(round((n_tg_norm_terkontrol / n_tg_norm) * 100, 0), decimal.mark=",")

# 8. Kalkulasi Korelasi untuk Narasi (Log CAC)
df_clean$log_CAC <- log(df_clean$Global_CAC_score + 1)

# Fungsi untuk menghitung dan format korelasi (SUDAH DIREVISI KE SPEARMAN)
get_cor <- function(var_name) {
  res <- suppressWarnings(cor.test(df_clean[[var_name]], df_clean$log_CAC, method = "spearman", exact = FALSE))
  r_val <- format(round(res$estimate, 3), decimal.mark=",")
  p_num <- res$p.value
  p_val <- ifelse(p_num < 0.001, "< 0,001", paste0("= ", format(round(p_num, 3), decimal.mark=",")))
  return(list(r = r_val, p = p_val))
}

# Ekstrak nilai korelasi masing-masing variabel
cor_tg <- get_cor("Trigliserida")
cor_tc <- get_cor("Kolesterol-Total")
cor_tghdl <- get_cor("TG_HDL_Ratio")
cor_nhdl <- get_cor("Non_HDL")
cor_usia <- get_cor("Usia")

# 9. Kalkulasi PGK untuk Narasi Pembahasan
n_hrp_neg <- sum(df_clean$HRP_Status == "HRP (-)", na.rm = TRUE)
n_pgk_pos_hrp <- sum(df_clean$CKD_Status == 1 & df_clean$HRP_Status == "HRP (+)", na.rm = TRUE)
n_pgk_neg_hrp <- sum(df_clean$CKD_Status == 1 & df_clean$HRP_Status == "HRP (-)", na.rm = TRUE)
pct_pgk_pos_hrp <- format(round((n_pgk_pos_hrp / n_hrp_pos) * 100, 0), decimal.mark=",")
pct_pgk_neg_hrp <- format(round((n_pgk_neg_hrp / n_hrp_neg) * 100, 0), decimal.mark=",")

# 10. Kalkulasi P-value dan AUC Ekstra untuk Bab Pembahasan
library(pROC)

# AUC Non-HDL
auc_nhdl <- format(round(auc(roc(df_clean$HRP_Status, df_clean$Non_HDL, quiet=TRUE)), 2), decimal.mark=",")

# P-value Bivariat Non-HDL vs HRP (Wilcoxon)
pval_nhdl_hrp <- format(round(wilcox.test(Non_HDL ~ HRP_Status, data = df_clean)$p.value, 3), decimal.mark=",")

# P-value Non-HDL Kat vs CAC (Wilcoxon)
df_clean$Non_HDL_Kat_Temp <- ifelse(df_clean$Non_HDL >= 130, "Tinggi", "Optimal")
pval_nhdl_cac <- format(round(wilcox.test(Global_CAC_score ~ Non_HDL_Kat_Temp, data = df_clean)$p.value, 3), decimal.mark=",")

# P-value Durasi DM vs Non-HDL Kat (Chi-Square)
pval_durasi_nhdl <- format(round(chisq.test(table(df_clean$Riwayat_DM, df_clean$Non_HDL_Kat_Temp), correct = FALSE)$p.value, 3), decimal.mark=",")

# P-value HbA1c vs TG/HDL (Chi-Square)
df_clean$TG_HDL_Kat_Temp <- ifelse(df_clean$TG_HDL_Ratio >= 3.0, "Tinggi", "Normal")
df_clean$HbA1C_Kat_Temp <- ifelse(df_clean$HbA1C >= 7.0, "Tidak Terkontrol", "Terkontrol")
pval_hba1c_tghdl <- format(round(chisq.test(table(df_clean$HbA1C_Kat_Temp, df_clean$TG_HDL_Kat_Temp), correct = FALSE)$p.value, 3), decimal.mark=",")

# 11. Kalkulasi Regresi Linear Parsial (Kontrol Usia)
mod_nhdl <- lm(log_CAC ~ Non_HDL + Usia, data = df_clean)
mod_tg <- lm(log_CAC ~ TG_HDL_Ratio + Usia, data = df_clean)

# Ekstrak P-value untuk narasi Supplementary
pval_nhdl_adj <- format(round(summary(mod_nhdl)$coefficients["Non_HDL", "Pr(>|t|)"], 3), decimal.mark=",")
pval_usia_adj1 <- format(round(summary(mod_nhdl)$coefficients["Usia", "Pr(>|t|)"], 3), decimal.mark=",")

pval_tg_adj <- format(round(summary(mod_tg)$coefficients["TG_HDL_Ratio", "Pr(>|t|)"], 3), decimal.mark=",")
pval_usia_adj2 <- format(round(summary(mod_tg)$coefficients["Usia", "Pr(>|t|)"], 3), decimal.mark=",")

# 12. Analisis Stratifikasi Statin (Eksploratif)
df_statin_yes <- df_clean %>% filter(Statin == "On-Statin")
df_statin_no <- df_clean %>% filter(Statin == "No-Statin")

fmt_cor <- function(res) {
  r_val <- format(round(res$estimate, 3), decimal.mark=",")
  p_num <- res$p.value
  p_val <- ifelse(p_num < 0.001, "< 0,001", paste0("= ", format(round(p_num, 3), decimal.mark=",")))
  return(list(r = r_val, p = p_val))
}

c_nhdl_sy <- fmt_cor(suppressWarnings(cor.test(df_statin_yes$Non_HDL, df_statin_yes$log_CAC, method = "spearman", exact = FALSE)))
c_nhdl_sn <- fmt_cor(suppressWarnings(cor.test(df_statin_no$Non_HDL, df_statin_no$log_CAC, method = "spearman", exact = FALSE)))
c_tg_sy <- fmt_cor(suppressWarnings(cor.test(df_statin_yes$TG_HDL_Ratio, df_statin_yes$log_CAC, method = "spearman", exact = FALSE)))
c_tg_sn <- fmt_cor(suppressWarnings(cor.test(df_statin_no$TG_HDL_Ratio, df_statin_no$log_CAC, method = "spearman", exact = FALSE)))

```

**Karakteristik Subjek, Profil Klinis, dan Analisis Bivariat**

Penelitian ini menganalisis total `r n_total` subjek pasien DM. Mayoritas pasien memiliki riwayat rujukan dari kardiolog akibat riwayat *cardiac events* sebelumnya, sehingga `r n_statin` pasien (`r pct_statin`%) tercatat telah menerima terapi statin. Dari segi durasi penyakit, `r n_dm_baru` pasien (`r pct_dm_baru`%) merupakan pasien yang baru terdiagnosis DM sebelum prosedur CTCA. Sementara itu, pada profil pengobatan antidiabetik, pasien terbagi merata antara pengguna obat oral (`r pct_oad`%) dan insulin (`r pct_ins`%), sedangkan sisanya (`r pct_diet`%) belum atau tidak menerima terapi medikamentosa (terdiri atas `r format(round(((n_diet - n_dm_baru) / n_total) * 100, 1), decimal.mark=",")`% pasien DM lama yang mengandalkan modifikasi diet dan `r pct_dm_baru`% pasien baru terdiagnosis). Berdasarkan uji normalitas Shapiro-Wilk, sebagian besar variabel numerik, termasuk skor kalsifikasi koroner (CAC), menunjukkan distribusi data yang tidak normal (p \< 0,05) (lampiran). Oleh karena itu, data kontinu pada penelitian ini disajikan dalam bentuk median dengan rentang interkuartil (Q1–Q3), uji non-parametrik (Mann-Whitney U), dan seluruh analisis korelasi lanjutan menggunakan uji non-parametrik Spearman's rank ($\rho$).

Berdasarkan pencitraan CTCA, proporsi penemuan *High-Risk Plaque* (HRP) yakni sebesar `r pct_hrp_pos`%. Seluruh karakteristik dasar, parameter laboratorium, dan profil pengobatan dievaluasi sekaligus terhadap dua luaran utama yakni status HRP (@tbl-baseline) dan skor kalsifikasi absolut (@tbl-cac). Analisis bivariat menunjukkan bahwa kelompok dengan HRP positif memiliki rerata usia yang secara signifikan lebih tua dibandingkan kelompok HRP negatif (`r mean_usia_pos` vs `r mean_usia_neg` tahun, $p = 0,010$). Skor kalsifikasi absolut juga menunjukkan perbedaan bermakna antar kelompok (median `r med_cac_pos` vs `r med_cac_neg` Agatston, $p < 0,001$).

Melihat tingginya persentase pasien yang telah menerima terapi medis spesifik, evaluasi komparatif tambahan terkait penggunaan statin menampilkan tren median skor CAC yang lebih tinggi dibandingkan pasien tanpa statin (`r med_cac_statin_pos` vs `r med_cac_statin_neg` Agatston). Begitu pula pada durasi penyakit, pasien dengan DM yang sudah lama menampilkan skor kalsifikasi yang jauh lebih tinggi dibandingkan pasien yang baru terdiagnosis. Di sisi lain, proporsi kerentanan plak (HRP) tertinggi secara persentase justru ditemukan pada kelompok yang belum menerima intervensi obat anti-diabetik (hanya mengandalkan diet).

Seluruh kandidat indeks kardiometabolik turunan (Indeks TyHbA1c, Rasio TG/HDL, Non-HDL, dan Rasio SUA/HDL) secara konsisten tidak menunjukkan perbedaan yang bermakna secara statistik antar-kelompok pada kohort terbatas ini. Mengingat hanya faktor penuaan (usia) yang memenuhi kriteria seleksi awal ($p < 0,05$), tahapan pemodelan regresi multivariat tidak dilanjutkan untuk menghindari *overfitting* pada data.

```{r}
#| label: tbl-baseline
#| tbl-cap: "**Karakteristik Dasar, Profil Klinis, dan Komparasi Indeks Kardiometabolik Berdasarkan Status High-Risk Plaque (HRP)**"

library(flextable)

df_table <- df_clean %>%
  mutate(
    CKD_Status = ifelse(CKD_Status == 1, "PGK stadium \u2265 3", "Normal/PGK \u2264 2"),
    Statin = factor(Statin, levels = c("No-Statin", "On-Statin"), labels = c("Tidak", "Ya")),
    Gender = factor(Gender, levels = c("L", "P"), labels = c("Laki-laki", "Perempuan")),
    # Membuat kategori baru untuk Tabel 2 nanti
    Usia_Kat = factor(ifelse(Usia >= 60, "\u2265 60 Tahun", "< 60 Tahun"), levels = c("< 60 Tahun", "\u2265 60 Tahun")),
    IMT_Kat = factor(ifelse(IMT >= 25, "Obesitas", "Normal/Overweight"), levels = c("Normal/Overweight", "Obesitas"))
  )

label_variabel_1 <- list(
  Usia ~ "Usia (Tahun)",
  Gender ~ "Jenis Kelamin",
  IMT ~ "Indeks Massa Tubuh (kg/m\u00b2)",
  Hipertensi_Kat ~ "Riwayat Hipertensi",
  Merokok ~ "Riwayat Merokok",
  Statin ~ "Penggunaan Statin",
  Terapi_DM ~ "Modalitas Terapi DM",
  Riwayat_DM ~ "Durasi Diagnosis DM",
  CKD_Status ~ "Status Fungsi Ginjal",
  Hemoglobin ~ "Hemoglobin (g/dL)",
  HbA1C ~ "HbA1c (%)",
  `Kolesterol-Total` ~ "Kolesterol Total (mg/dL)",
  LDL ~ "Kolesterol LDL (mg/dL)",
  HDL ~ "Kolesterol HDL (mg/dL)",
  Trigliserida ~ "Trigliserida (mg/dL)",
  Kreatinin ~ "Serum Kreatinin (mg/dL)",
  `Asam-Urat` ~ "Asam Urat Serum (mg/dL)",
  Global_CAC_score ~ "Skor CAC Aktual (Agatston)",
  TyHbA1c_Index ~ "Indeks TyHbA1c",
  TG_HDL_Ratio ~ "Rasio TG / HDL-C",
  Non_HDL ~ "Kolesterol Non-HDL (mg/dL)",
  SUA_HDL_Ratio ~ "Rasio Asam Urat / HDL-C"
)

df_table %>%
  select(HRP_Status, Usia, Gender, IMT, Hipertensi_Kat, Merokok, Statin, Terapi_DM, Riwayat_DM, 
         CKD_Status, Hemoglobin, HbA1C, `Kolesterol-Total`, LDL, HDL, Trigliserida, Kreatinin, `Asam-Urat`,
         Global_CAC_score, TyHbA1c_Index, TG_HDL_Ratio, Non_HDL, SUA_HDL_Ratio) %>%
  tbl_summary(
    by = HRP_Status, 
    statistic = list(all_continuous() ~ "{median} ({p25}, {p75})"),
    label = label_variabel_1,
    missing = "no"
  ) %>%
  add_overall() %>%
  add_p() %>%
  bold_p(t = 0.05) %>%
  modify_header(label = "**Karakteristik**") %>%
  as_flex_table()
```

```{r}
#| label: tbl-cac
#| tbl-cap: "**Evaluasi Karakteristik Klinis dan Laboratorium Dasar terhadap Distribusi Beban Kalsifikasi Koroner (Skor CAC)**"
#| warning: false
#| message: false

library(gtsummary)
library(flextable)
library(dplyr)
library(tibble)

# 1. Membuat Kategori Lab Dasar & Menambahkan Jumlah (n)
df_cac <- df_table %>%
  mutate(
    HbA1C_Kat = factor(ifelse(HbA1C >= 7.0, "\u2265 7.0% (Tidak Terkontrol)", "< 7.0% (Terkontrol)"), 
                       levels = c("< 7.0% (Terkontrol)", "\u2265 7.0% (Tidak Terkontrol)")),
    Kolesterol_Kat = factor(ifelse(`Kolesterol-Total` >= 200, "\u2265 200 mg/dL (Tinggi)", "< 200 mg/dL (Normal)"), 
                            levels = c("< 200 mg/dL (Normal)", "\u2265 200 mg/dL (Tinggi)")),
    LDL_Kat = factor(ifelse(LDL >= 100, "\u2265 100 mg/dL (Tidak Optimal)", "< 100 mg/dL (Optimal)"), 
                     levels = c("< 100 mg/dL (Optimal)", "\u2265 100 mg/dL (Tidak Optimal)")),
    HDL_Kat = factor(case_when(
      Gender == "Laki-laki" & HDL < 40 ~ "Rendah",
      Gender == "Perempuan" & HDL < 50 ~ "Rendah",
      TRUE ~ "Normal"
    ), levels = c("Normal", "Rendah")),
    TG_Kat = factor(ifelse(Trigliserida >= 150, "\u2265 150 mg/dL (Tinggi)", "< 150 mg/dL (Normal)"), 
                    levels = c("< 150 mg/dL (Normal)", "\u2265 150 mg/dL (Tinggi)")),
    AsamUrat_Kat = factor(case_when(
      Gender == "Laki-laki" & `Asam-Urat` > 7.0 ~ "Hiperurisemia",
      Gender == "Perempuan" & `Asam-Urat` > 6.0 ~ "Hiperurisemia",
      TRUE ~ "Normal"
    ), levels = c("Normal", "Hiperurisemia")),
    HGB_Kat = factor(case_when(
      Gender == "Laki-laki" & Hemoglobin < 13.0 ~ "Anemia",
      Gender == "Perempuan" & Hemoglobin < 12.0 ~ "Anemia",
      TRUE ~ "Normal"
    ), levels = c("Normal", "Anemia"))
  ) %>%
  mutate(across(
    c(HRP_Status, Usia_Kat, Gender, IMT_Kat, Hipertensi_Kat, Merokok, 
      Statin, Terapi_DM, Riwayat_DM, CKD_Status,
      HGB_Kat, HbA1C_Kat, Kolesterol_Kat, LDL_Kat, HDL_Kat, TG_Kat, AsamUrat_Kat),
    ~ {
      fac <- as.factor(.x)
      lvls <- levels(fac)
      counts <- table(fac)
      new_labels <- paste0(lvls, " (n=", as.numeric(counts[lvls]), ")")
      factor(fac, levels = lvls, labels = new_labels)
    }
  ))

label_variabel_2 <- list(
  HRP_Status ~ "Status High-Risk Plaque (HRP)",
  Usia_Kat ~ "Kategori Usia",
  Gender ~ "Jenis Kelamin",
  IMT_Kat ~ "Kategori IMT",
  Hipertensi_Kat ~ "Riwayat Hipertensi",
  Merokok ~ "Riwayat Merokok",
  Statin ~ "Penggunaan Statin",
  Terapi_DM ~ "Modalitas Terapi DM",
  Riwayat_DM ~ "Durasi Diagnosis DM",
  CKD_Status ~ "Status Fungsi Ginjal",
  HGB_Kat ~ "Hemoglobin",
  HbA1C_Kat ~ "HbA1c",
  Kolesterol_Kat ~ "Kolesterol Total",
  LDL_Kat ~ "Kolesterol LDL",
  HDL_Kat ~ "Kolesterol HDL",
  TG_Kat ~ "Trigliserida",
  AsamUrat_Kat ~ "Asam Urat Serum"
)

# 3. FUNGSI KUSTOM (Revisi: SPEARMAN CORRELATION)
korelasi_kontinu <- function(data, variable, ...) {
  kamus_var <- c(
    "Usia_Kat" = "Usia",
    "IMT_Kat" = "IMT",
    "HbA1C_Kat" = "HbA1C",
    "Kolesterol_Kat" = "Kolesterol-Total",
    "LDL_Kat" = "LDL",
    "HDL_Kat" = "HDL",
    "TG_Kat" = "Trigliserida",
    "AsamUrat_Kat" = "Asam-Urat",
    "HGB_Kat" = "Hemoglobin"
  )
  
  var_kontinu <- kamus_var[[variable]]
  if (is.null(var_kontinu) || !var_kontinu %in% names(data)) {
    return(tibble(cor_stat = NA_character_, is_sig = FALSE))
  }
  
  data$log_CAC <- log(data$Global_CAC_score + 1)
  valid <- !is.na(data[[var_kontinu]]) & !is.na(data$log_CAC)
  if (sum(valid) < 3) return(tibble(cor_stat = NA_character_, is_sig = FALSE))
  
  # MENGGUNAKAN SPEARMAN
  res <- suppressWarnings(cor.test(data[[var_kontinu]][valid], data$log_CAC[valid], 
                                   method = "spearman", exact = FALSE))
  
  r_str <- format(round(res$estimate, 3), decimal.mark=",")
  p_str <- ifelse(res$p.value < 0.001, "< 0,001", format(round(res$p.value, 3), decimal.mark=","))
  sig_flag <- res$p.value < 0.05
  stat_str <- paste0(r_str, " (p=", p_str, ")")
  
  tibble(cor_stat = stat_str, is_sig = sig_flag)
}

# 4. Membuat Tabel Final
df_cac %>%
  tbl_continuous(
    variable = Global_CAC_score,
    include = c(HRP_Status, Usia_Kat, Gender, IMT_Kat, Hipertensi_Kat, Merokok, 
                Statin, Terapi_DM, Riwayat_DM, CKD_Status,
                HGB_Kat, HbA1C_Kat, Kolesterol_Kat, LDL_Kat, HDL_Kat, TG_Kat, AsamUrat_Kat),
    statistic = ~ "{median} ({p25}, {p75})",
    label = label_variabel_2
  ) %>%
  add_p() %>%
  add_stat(fns = everything() ~ korelasi_kontinu) %>%
  modify_header(
    label ~ "**Karakteristik / Variabel**",
    stat_0 ~ "**Median (Q1–Q3)**", 
    p.value ~ "**Nilai p**",
    cor_stat ~ "**Korelasi Spearman**\n*(rho, p-value)*" # Diubah ke Spearman
  ) %>%
  modify_table_styling(columns = cor_stat, rows = is_sig == TRUE, text_format = "bold") %>%
  modify_column_hide(columns = any_of(c("add_stat_1", "addstat1", "is_sig"))) %>%
  bold_p(t = 0.05) %>%
  as_flex_table() %>%
  flextable::align(align = "center", part = "header") %>%
  flextable::autofit()
```

**Evaluasi Independen Indeks Kardiometabolik Turunan**

Evaluasi secara spesifik hubungan indeks kardiometabolik turunan (sebagai penanda resistensi insulin dan disfungsi endotel) dengan arteriosklerosis dilakukan analisis bivariat (**Tabel 3**). Pada evaluasi beban kalsifikasi, kolesterol Non-HDL menunjukkan asosiasi terbalik yang signifikan secara statistik ($p = 0,037$), di mana kelompok dengan Non-HDL optimal (\< 130 mg/dL) memiliki median kalsifikasi yang lebih tinggi (398 Agatston) dibandingkan kelompok Non-HDL tinggi (70 Agatston). Sementara itu, indeks lainnya (Indeks TyHbA1c, Rasio TG/HDL, dan Rasio SUA/HDL) secara konsisten tidak memiliki hubungan yang bermakna secara statistik terhadap derajat kalsifikasi. Terkait status kerentanan plak (HRP), tidak ada indeks kardiometabolik yang menunjukkan diskriminasi bermakna ($p > 0,05$).

```{r}
#| label: tbl-metabolik
#| tbl-cap: "**Evaluasi Indeks Kardiometabolik Turunan terhadap Kerentanan Plak (HRP) dan Beban Kalsifikasi Koroner (CAC)**"
#| warning: false
#| message: false

library(gtsummary)
library(flextable)
library(dplyr)
library(tibble)

# 1. Hitung median untuk TyHbA1c dan SUA/HDL
med_ty <- median(df_clean$TyHbA1c_Index, na.rm = TRUE)
med_sua <- median(df_clean$SUA_HDL_Ratio, na.rm = TRUE)

df_metab <- df_clean %>%
  mutate(
    Non_HDL_Kat = factor(ifelse(Non_HDL >= 130, "\u2265 130 mg/dL (Tinggi)", "< 130 mg/dL (Optimal)"), 
                         levels = c("< 130 mg/dL (Optimal)", "\u2265 130 mg/dL (Tinggi)")),
    TG_HDL_Kat = factor(ifelse(TG_HDL_Ratio >= 3.0, "\u2265 3.0 (Tinggi)", "< 3.0 (Normal)"), 
                        levels = c("< 3.0 (Normal)", "\u2265 3.0 (Tinggi)")),
    TyHbA1c_Kat = factor(ifelse(TyHbA1c_Index > med_ty, "> Median (Tinggi)", "\u2264 Median (Rendah)"), 
                         levels = c("\u2264 Median (Rendah)", "> Median (Tinggi)")),
    SUA_HDL_Kat = factor(ifelse(SUA_HDL_Ratio > med_sua, "> Median (Tinggi)", "\u2264 Median (Rendah)"), 
                         levels = c("\u2264 Median (Rendah)", "> Median (Tinggi)"))
  ) %>%
  mutate(across(
    c(Non_HDL_Kat, TG_HDL_Kat, TyHbA1c_Kat, SUA_HDL_Kat),
    ~ {
      fac <- as.factor(.x)
      lvls <- levels(fac)
      counts <- table(fac)
      new_labels <- paste0(lvls, " (n=", as.numeric(counts[lvls]), ")")
      factor(fac, levels = lvls, labels = new_labels)
    }
  )) %>%
  select(HRP_Status, Global_CAC_score, 
         Non_HDL_Kat, TG_HDL_Kat, TyHbA1c_Kat, SUA_HDL_Kat,
         Non_HDL, TG_HDL_Ratio, TyHbA1c_Index, SUA_HDL_Ratio)

label_metab <- list(
  Non_HDL_Kat ~ "Kolesterol Non-HDL",
  TG_HDL_Kat ~ "Rasio TG / HDL-C",
  TyHbA1c_Kat ~ "Indeks TyHbA1c",
  SUA_HDL_Kat ~ "Rasio Asam Urat / HDL-C"
)

# 4. FUNGSI KUSTOM (Revisi: SPEARMAN CORRELATION)
korelasi_indeks <- function(data, variable, ...) {
  kamus_var <- c(
    "Non_HDL_Kat" = "Non_HDL",
    "TG_HDL_Kat" = "TG_HDL_Ratio",
    "TyHbA1c_Kat" = "TyHbA1c_Index",
    "SUA_HDL_Kat" = "SUA_HDL_Ratio"
  )
  
  var_kontinu <- kamus_var[[variable]]
  if (is.null(var_kontinu) || !var_kontinu %in% names(data)) {
    return(tibble(cor_stat = NA_character_, is_sig = FALSE))
  }
  
  data$log_CAC <- log(data$Global_CAC_score + 1)
  valid <- !is.na(data[[var_kontinu]]) & !is.na(data$log_CAC)
  if (sum(valid) < 3) return(tibble(cor_stat = NA_character_, is_sig = FALSE))
  
  # MENGGUNAKAN SPEARMAN
  res <- suppressWarnings(cor.test(data[[var_kontinu]][valid], data$log_CAC[valid], 
                                   method = "spearman", exact = FALSE))
  
  r_str <- format(round(res$estimate, 3), decimal.mark=",")
  p_str <- ifelse(res$p.value < 0.001, "< 0,001", format(round(res$p.value, 3), decimal.mark=","))
  sig_flag <- res$p.value < 0.05
  stat_str <- paste0(r_str, " (p=", p_str, ")")
  
  tibble(cor_stat = stat_str, is_sig = sig_flag)
}

# 5. Tabel Sisi Kiri
tbl_hrp_metab <- df_metab %>%
  select(HRP_Status, Non_HDL_Kat, TG_HDL_Kat, TyHbA1c_Kat, SUA_HDL_Kat) %>%
  tbl_summary(by = HRP_Status, label = label_metab, missing = "no") %>%
  add_p() %>%
  modify_header(p.value ~ "**Nilai p**") %>%
  bold_p(t = 0.05)

# 6. Tabel Sisi Kanan (Hibrida)
tbl_cac_metab <- df_metab %>%
  select(-HRP_Status) %>%
  tbl_continuous(
    variable = Global_CAC_score,
    include = c(Non_HDL_Kat, TG_HDL_Kat, TyHbA1c_Kat, SUA_HDL_Kat),
    statistic = ~ "{median} ({p25}, {p75})",
    label = label_metab
  ) %>%
  add_p() %>%
  add_stat(fns = everything() ~ korelasi_indeks) %>%
  modify_header(
    stat_0 ~ "**Median (Q1–Q3)**",       
    p.value ~ "**Nilai p**",             
    cor_stat ~ "**Korelasi Spearman**\n*(rho, p-value)*" # Diubah ke Spearman
  ) %>%
  modify_table_styling(columns = cor_stat, rows = is_sig == TRUE, text_format = "bold") %>%
  modify_column_hide(columns = any_of(c("add_stat_1", "addstat1", "is_sig"))) %>%
  bold_p(t = 0.05)

# 7. Gabungkan Kedua Tabel
tbl_merge(
  tbls = list(tbl_hrp_metab, tbl_cac_metab),
  tab_spanner = c("**Status Plak (HRP)**", "**Beban Kalsifikasi (CAC)**")
) %>% 
  modify_header(label = "**Indeks Kardiometabolik**") %>%
  bold_labels() %>%
  as_flex_table() %>%
  flextable::align(align = "center", part = "header") %>%
  flextable::autofit()
```

**Analisis Indeks Non-HDL dan Rasio TG/HDL**

Analisis korelasi terbalik pada kolesterol Non-HDL dilanjutkan dengan analisis komparasi silang (cross-tabulation) dilakukan terhadap seluruh profil klinis pasien (@tbl-nonhdl). Hasil analisis menujukan tidak adanya hubungan terkait penggunaan statin ($p=0,2$), melainkan angka Non-HDL secara signifikan berhubungan dengan durasi terdiagnosis DM ($p=0,029$). Kelompok pasien dengan Kolesterol Non-HDL yang optimal (\< 130 mg/dL) sebagian besar (`r pct_lama_opt`%) adalah pasien dengan riwayat DM yang sudah lama.

Analisis terpisah pada Rasio TG/HDL (@tbl-tghdl) menunjukan hubungan dengan kontrol glikemik. Kelompok pasien dengan Rasio TG/HDL yang normal (\< 3,0) secara signifikan memiliki proporsi **kontrol glikemik (HbA1c)** yang lebih baik (`r pct_hba1c_tg_norm`% terkontrol, $p=0,044$) dibandingkan kelompok dengan rasio tinggi. Hal ini mendukung Rasio TG/HDL sebagai penanda surrogate resistensi insulin yang representatif, meskipun indeks tersebut gagal mengungguli faktor degeneratif (usia) dalam memprediksi kerentanan plak dan kalsifikasi koroner.

Untuk memverifikasi ketahanan (*robustness*) dari temuan ini, analisis lanjutan berupa evaluasi performa diagnostik (Kurva ROC) dan korelasi linear komprehensif turut dilakukan. Kurva ROC mengonfirmasi daya diskriminasi keempat indeks kardiometabolik yang lemah (AUC \< 0,70) terhadap HRP (@fig-supp-roc).

Lebih lanjut, analisis korelasi Spearman terhadap skor CAC logaritmik (Gambar S3) justru menunjukan pola korelasi negatif yang tidak bermakna signifikan secara statistik pada parameter lipid dan indeks turunannya, seperti Trigliserida ($\rho$ = `r cor_tg$r`, $p$ `r cor_tg$p`), Kolesterol Total ($\rho$ = `r cor_tc$r`, $p$ `r cor_tc$p`), Rasio TG/HDL ($\rho$ = `r cor_tghdl$r`, $p$ `r cor_tghdl$p`), dan Non-HDL ($\rho$ = `r cor_nhdl$r`, $p$ `r cor_nhdl$p`). Hal ini secara kuantitatif mendukung hipotesis metabolic memory yakni kalsifikasi yang semakin berat sejalan dengan intensitas kontrol lipid (efek statin kronis) saat ini, sehingga indeks metabolik sirkulasi menampilkan ilusi "berkorelasi terbalik" dan kehilangan relevansinya sebagai prediktor linier independen pada fase penyakit lanjut.

```{r}
#| label: tbl-nonhdl
#| tbl-cap: "**Karakteristik Klinis dan Profil Pengobatan Berdasarkan Kategori Kolesterol Non-HDL**"

df_nhdl <- df_cac %>%
  mutate(
    Non_HDL_Kat = factor(ifelse(Non_HDL >= 130, "\u2265 130 mg/dL (Tinggi)", "< 130 mg/dL (Optimal)"), 
                         levels = c("< 130 mg/dL (Optimal)", "\u2265 130 mg/dL (Tinggi)"))
  ) %>%
  # Menambahkan Usia_Kat di urutan pertama
  select(Non_HDL_Kat, Usia_Kat, Gender, IMT_Kat, Hipertensi_Kat, Merokok, HbA1C_Kat, Riwayat_DM, Terapi_DM, Statin)

label_proof <- list(
  Usia_Kat ~ "Kategori Usia",
  Gender ~ "Jenis Kelamin",
  IMT_Kat ~ "Kategori IMT",
  Hipertensi_Kat ~ "Status Hipertensi",
  Merokok ~ "Riwayat Merokok",
  HbA1C_Kat ~ "Kontrol Glikemik (HbA1c)",
  Riwayat_DM ~ "Durasi Diagnosis DM",
  Terapi_DM ~ "Modalitas Terapi DM",
  Statin ~ "Penggunaan Statin"
)

df_nhdl %>%
  tbl_summary(by = Non_HDL_Kat, label = label_proof, missing = "no") %>%
  add_p() %>% 
  bold_p(t = 0.05) %>%
  modify_header(label = "**Faktor Klinis / Pengobatan**") %>%
  as_flex_table()
```

```{r}
#| label: tbl-tghdl
#| tbl-cap: "**Karakteristik Klinis dan Profil Pengobatan Berdasarkan Kategori Rasio TG/HDL-C**"

library(flextable)

df_tg <- df_cac %>%
  mutate(
    TG_HDL_Kat = factor(ifelse(TG_HDL_Ratio >= 3.0, "\u2265 3.0 (Tinggi)", "< 3.0 (Normal)"), 
                        levels = c("< 3.0 (Normal)", "\u2265 3.0 (Tinggi)"))
  ) %>%
  # Menambahkan Usia_Kat di urutan pertama
  select(TG_HDL_Kat, Usia_Kat, Gender, IMT_Kat, Hipertensi_Kat, Merokok, HbA1C_Kat, Riwayat_DM, Terapi_DM, Statin)

df_tg %>%
  tbl_summary(by = TG_HDL_Kat, label = label_proof, missing = "no") %>%
  add_p() %>% 
  bold_p(t = 0.05) %>%
  modify_header(label = "**Faktor Klinis / Pengobatan**") %>%
  as_flex_table()
```

**Evaluasi Efek Perancu (*Confounding*) dan Fenomena Paradoks Statin (*Statin Paradox*)**

Untuk mengevaluasi hubungan kolesterol Non-HDL dan Rasio TG/HDL terhadap kalsifikasi koroner, analisis regresi linear multivariat sederhana dilakukan. Model regresi linier multivariat disusun untuk memprediksi kalsifikasi ($\ln[\text{CAC}+1]$) menggunakan indeks Kolesterol Non-HDL dan Rasio TG/HDL-C dengan mengontrol faktor usia (@tbl-supp-regresi). Efek korelasi negatif dari kolesterol Non-HDL ($p = `r pval_nhdl_adj`$) dan Rasio TG/HDL ($p = `r pval_tg_adj`$) ternyata tetap signifikan secara statistik setelah dikontrol usia. Hal ini membuktikan secara objektif bahwa Usia bukanlah satu-satunya confounder. Persistensi korelasi negatif yang independen terhadap usia ini mengindikasikan kuat adanya bias perancu lain di luar faktor usia.

```{r}
#| label: tbl-supp-regresi
#| tbl-cap: "**Analisis Regresi Linear terhadap Log Kalsifikasi (Mengontrol Efek Perancu Usia)**"


# Model 1: Non-HDL + Usia
tbl_mod_nhdl <- tbl_regression(mod_nhdl, label = list(Non_HDL ~ "Kolesterol Non-HDL", Usia ~ "Usia (Tahun)")) %>%
  bold_p(t = 0.05)

# Model 2: TG/HDL + Usia
tbl_mod_tg <- tbl_regression(mod_tg, label = list(TG_HDL_Ratio ~ "Rasio TG/HDL-C", Usia ~ "Usia (Tahun)")) %>%
  bold_p(t = 0.05)

# KUNCI PERBAIKAN: Gunakan tbl_stack dengan header deskriptif dan tambah catatan kaki
tbl_stack(
  tbls = list(tbl_mod_nhdl, tbl_mod_tg),
  # 1. Ubah teks header agar lebih jelas
  group_header = c("Model 1: Evaluasi Kolesterol Non-HDL", 
                   "Model 2: Evaluasi Rasio TG/HDL-C")
) %>%
  modify_header(label = "**Variabel Prediktor**") %>%
  as_flex_table() %>%
  # 2. Tambahkan catatan kaki (footnote) di bagian bawah tabel
  flextable::add_footer_lines("Keterangan: Model 1 dan Model 2 dianalisis secara terpisah (parsial) untuk memprediksi nilai logaritma kalsifikasi koroner (ln[CAC+1]) dengan mengontrol variabel perancu (Usia).")
```

**Stratifikasi Berdasarkan Penggunaan Statin**

Analisis stratifikasi eksploratif dilakukan untuk membandingkan korelasi indeks kardiometabolik terhadap kalsifikasi secara terpisah antara kelompok dengan terapi statin ($n=$ `r n_statin`) dan tanpa statin ($n=$ `r n_total - n_statin`). Analisis korelasi Spearman (@fig-supp-statin) parameter Rasio TG/HDL-C menunjukkan koefisien korelasi negatif yang lebih besar pada kelompok tanpa statin ($\rho=$ `r c_tg_sn$r`) dibandingkan pengguna statin ($\rho=$ `r c_tg_sy$r`). Kehilangan signifikansi statistik pada kelompok tanpa statin ($p$ `r c_tg_sn$p`) lebih mencerminkan keterbatasan kekuatan sampel (power) akibat ukuran kohort yang kecil ($n=$ `r n_total - n_statin`) daripada hilangnya arah asosiasi. Hal ini terbukti secara konsisten pada parameter kolesterol Non-HDL, di mana korelasi negatif yang bermakna secara statistik tetap bertahan secara tangguh pada kelompok tanpa statin ($\rho=$ `r c_nhdl_sn$r`, $p$ `r c_nhdl_sn$p`), sementara pada kelompok pengguna statin korelasi tersebut melemah dan menjadi tidak bermakna ($\rho=$ `r c_nhdl_sy$r`, $p$ `r c_nhdl_sy$p`). Meskipun temuan ini didapatkan pada ukuran sampel yang terbatas ($n=$ `r n_total - n_statin`), perbedaan mencolok ini semakin menguatkan indikasi adanya efek perancu farmakologis dari terapi statin kronis (statin paradox) terhadap parameter lipid sirkulasi pasien.

```{r}
#| label: fig-supp-statin
#| fig-cap: "Analisis korelasi kolesterol non-HDL dan Rasio TG/HDL-C terhadap kalsifikasi berdasarkan penggunaan Statin. Titik sebaran diwarnai berdasarkan usia pasien (spektrum biru untuk usia lebih muda, hingga merah untuk usia lebih tua), sementara bentuk titik membedakan durasi diabetes mellitus."
#| fig-width: 10
#| fig-height: 7.5
#| warning: false
#| message: false

library(ggplot2)
library(dplyr)
library(tidyr)

# 1. Menyiapkan data untuk facet (Tambahkan Durasi DM Kat)
df_strat <- df_clean %>%
  mutate(
    # Membuat kategori baru berdasarkan bulan (0 = baru, <60 = <5 tahun, >=60 = >=5 tahun)
    Durasi_DM_Kat = case_when(
      Durasi_DM_Bulan == 0 ~ "Baru Terdiagnosis",
      Durasi_DM_Bulan > 0 & Durasi_DM_Bulan < 60 ~ "DM < 5 Tahun",
      Durasi_DM_Bulan >= 60 ~ "DM \u2265 5 Tahun",
      TRUE ~ NA_character_
    ),
    Durasi_DM_Kat = factor(Durasi_DM_Kat, levels = c("Baru Terdiagnosis", "DM < 5 Tahun", "DM \u2265 5 Tahun"))
  ) %>%
  select(log_CAC, Statin, Non_HDL, TG_HDL_Ratio, Usia, Durasi_DM_Kat) %>%
  mutate(
    Statin = factor(Statin, levels = c("No-Statin", "On-Statin"), labels = c("Tanpa Statin (n=15)", "Pengguna Statin (n=37)")),
    `Kolesterol Non-HDL` = Non_HDL,
    `Rasio TG/HDL-C` = TG_HDL_Ratio
  ) %>%
  select(-Non_HDL, -TG_HDL_Ratio) %>%
  pivot_longer(cols = c(`Kolesterol Non-HDL`, `Rasio TG/HDL-C`), names_to = "Variable", values_to = "Value") %>%
  mutate(Variable = factor(Variable, levels = c("Kolesterol Non-HDL", "Rasio TG/HDL-C")))

# 2. Menghitung statistik untuk text label (DIREVISI KE SPEARMAN)
df_strat_stats <- df_strat %>%
  filter(!is.na(Value) & !is.na(log_CAC)) %>%
  group_by(Variable, Statin) %>%
  summarise(
    # Menggunakan Spearman dan meredam peringatan "ties" (angka kembar)
    rho = suppressWarnings(cor(Value, log_CAC, method = "spearman")),
    p_val = suppressWarnings(cor.test(Value, log_CAC, method = "spearman", exact = FALSE)$p.value),
    .groups = "drop"
  ) %>%
  mutate(
    p_text = ifelse(p_val < 0.001, "< 0.001", sprintf("= %.3f", p_val)),
    # Mengganti R menjadi rho
    label_text = sprintf("rho = %.3f\np %s", rho, p_text),
    # Menentukan style huruf: tebal untuk p < 0.05, biasa untuk p >= 0.05
    font_style = ifelse(p_val < 0.05, "bold", "plain")
  )

# 3. Membuat Plot Stratifikasi (Warna = Usia, Bentuk = Durasi DM)
p_strat <- ggplot(df_strat, aes(x = Value, y = log_CAC)) +
  # KUNCI: shape = Durasi_DM_Kat, ukuran titik diperbesar sedikit agar shape terlihat jelas
  geom_point(aes(color = Usia, shape = Durasi_DM_Kat), size = 3.5, alpha = 0.85) + 
  scale_color_gradientn(
    colors = c("#2b83ba", "#abdda4", "#fdae61", "#d7191c"), 
    name = "Usia\n(Tahun)"
  ) +
  # Menentukan jenis shape (16 = bulat penuh, 17 = segitiga penuh, 15 = kotak penuh)
  scale_shape_manual(
    values = c(16, 17, 15), 
    name = "Durasi DM"
  ) +
  geom_smooth(method = "lm", se = TRUE, color = "#c0392b", fill = "#f5b7b1", linewidth = 1) +
  # Memasukkan font_style ke dalam aesthetic (aes)
  geom_text(data = df_strat_stats, aes(x = Inf, y = Inf, label = label_text, fontface = font_style),
            hjust = 1.1, vjust = 1.2, size = 3.5, color = "black", inherit.aes = FALSE) +
  facet_grid(Statin ~ Variable, scales = "free_x") +
  labs(
    x = "Nilai Parameter", 
    y = "ln(Global CAC Score + 1)"
  ) +
  theme_bw(base_size = 12) +
  theme(
    strip.background = element_rect(fill = "#eaeded", color = "black"),
    strip.text = element_text(face = "bold", size = 11),
    panel.grid.minor = element_blank(),
    legend.position = "right",
    legend.title = element_text(face = "bold", size = 10),
    legend.box = "vertical",
    legend.spacing.y = unit(0.5, "cm")
  )

p_strat
```


## Pembahasan

Penelitian ini mengevaluasi faktor klinis serta laboratorium terhadap derjat aterosklerosis koroner. Faktor usia merupakan prediktor independen yang mendominasi kerentanan plak maupun kalsifikasi koroner, sementara indeks metabolik gagal menunjukkan efek independen.

**Dominansi Usia dan Waktu Paparan (*Time-Dependent Effect*)**

Usia sebagai satu-satunya determinan klinis yang mencapai signifikansi statistik mendukung karakter natural aterosklerosis sebagai kondisi patologis yang bergantung pada waktu (*time-dependent*). Semakin bertambah usia, semakin lama durasi sel endotel vaskular mengalami stres oksidatif dan disfungsi mekanik. Makrofag yang teraktivasi di ruang subendotel akan mensekresi *Matrix Metalloproteinases* (MMP) secara masif untuk mendegradasi matriks ekstraseluler pada tunika media.[@newby2008metalloproteinase] Degradasi matriks inilah yang mendasari kompensasi pembesaran lumen atau Fenomena Glagov (*positive remodeling*), salah satu fitur patognomonik dari plak berisiko tinggi (HRP).[@glagov1987compensatory] Komplikasi ginjal (PGK stadium $\ge$ 3) secara persentase menampilkan tren klinis peningkatan risiko pada kelompok HRP positif dibandingkan HRP negatif (`r pct_pgk_pos_hrp`% vs `r pct_pgk_neg_hrp`%), namun belum mencapai kemaknaan statistik, kemungkinan besar akibat sampel yang terbatas ($N = `r n_total`$). Ketiadaan parameter dengan nilai $p < 0,05$ pada uji bivariat di luar faktor usia juga mendasari tidak dilanjutkannya pemodelan multivariat, guna menghindari bias artefak pada kesimpulan akhir.

**Analisis Indeks Kardiometabolik**

Di antara keempat kandidat indeks kardiometabolik, kolesterol Non-HDL konsisten menunjukan signifikansi korelasi negatif (Spearman's rank) terhadap logaritma skor kalsifikasi ($\rho$ = `r cor_nhdl$r`, $p$ `r cor_nhdl$p`). Di sisi lain, indeks turunan yang diangkat untuk merepresentasikan resistensi insulin seperti TyHbA1c dan Rasio SUA/HDL tidak menunjukan hubungan bermakna. Nilai kolesterol non-HDL yang “optimal” justru memiliki skor CAC yang jauh lebih tinggi ($p =$ `r pval_nhdl_cac`) ketika dievaluasi melalui analisis tabulasi silang yang menunjukan adanya efek durasi diagnosis DM ($p =$ `r pval_durasi_nhdl`). Pasien dengan profil lipid yang "tampak optimal" di laboratorium ternyata adalah mereka yang telah mengidap DM dalam waktu yang sangat lama (`r pct_lama_opt`%). Hal ini merupakan manifestasi klinis klasik dari fenomena metabolic memory (memori metabolik) dan legacy effect. Kerusakan vaskular dan deposisi kalsium telah berjalan selama bertahun-tahun akibat hiperglikemia kronis sebelumnya yang meninggalkan “jejak” kerusakan oksidatif persisten pada dinding endotel. Walaupun pada saat prosedur CTCA profil lipid sirkulasi sudah ditekan hingga mencapai target (baik melalui perbaikan diet kronis maupun medikamentosa), kalsifikasi koroner sudah terbentuk secara permanen sehingga menciptakan fenomena “paradoks lipid”. Analisis regresi linear parsial lebih lanjut mengonfirmasi bahwa korelasi negatif parameter lipid ini tetap signifikan secara independen meskipun telah dikontrol terhadap faktor usia (Tabel 4).

Adanya hubungan antara Rasio TG/HDL yang normal dengan pencapaian kontrol glikemik (HbA1c) yang baik (`r pct_hba1c_tg_norm`% terkontrol, $p =$ `r pval_hba1c_tghdl`) mendukung status resistensi insulin dan kontrol metabolik sesaat. Namun, ketiadaan asosiasi linear Rasio TG/HDL terhadap luaran HRP dan CAC kembali menunjukkan bahwa marker metabolik sirkulasi tidak mampu melampaui kekuatan faktor degeneratif (usia) dan waktu paparan (time-dependent) dalam memprediksi perubahan struktural vaskular permanen pada populasi DM.

Indeks turunan berbasis resistensi insulin (seperti TyHbA1c dan Rasio TG/HDL) belum dapat mengungguli faktor usia pada studi ini tampak kontradiktif dengan beberapa literatur sebelumnya. Penelitian oleh Won et al. (2018) dan Jin et al. (2020) melaporkan hubungan positif yang kuat dan independen antara indeks kardiometabolik turunan dengan tingkat aterosklerosis koroner [@jin2020triglyceride; @won2018relationship].Diskrepansi hasil ini dapat dijelaskan melalui perbedaan mendasar pada profil intervensi medis dan fase kronisitas penyakit sampel. Studi-studi terdahulu mayoritas mengevaluasi populasi asimptomatik yang relatif “naif” terhadap terapi kardiovaskular, di mana kemungkinan resistensi insulin fase akut masih berkorelasi kuat dengan inisiasi awal plak. Sebaliknya, kohort dalam penelitian ini merepresentasikan kondisi rutin di Indonesia (real-world) berupa pasien rujukan kardiologi dengan durasi penyakit lanjut dan telah mendapatkan terapi statin. Pengaruh farmakologis dari statin menekan komponen lipid pembentuk indeks tersebut di sirkulasi darah (terutama trigliserida), sehingga secara artifisial “menumpulkan” nilai prediktifnya. Selain itu, keterbatasan desain potong lintang (cross-sectional) hanya mampu mencatat profil lipid sewaktu saat CTCA yang sebagian besar telah termodifikasi oleh terapi, dan gagal menangkap puncak resistensi insulin di masa lalu yang mungkin menjadi pemicu awal kalsifikasi sebelumnya. Oleh karena itu, uji kohort skala besar lebih lanjut yang mampu merekam fluktuasi lipid (lipid variability) dan pencatatan dosis akumulatif statin sangat disarankan untuk memvalidasi temuan ini.

## Simpulan

Pada pasien DM yang menjalani CTCA, faktor degeneratif (usia) bertindak sebagai determinan utama berhubungan dengan kerentanan plak (HRP) maupun kuantitas kalsifikasi koroner. Sebaliknya, penggunaan indeks kardiometabolik turunan baru sebagai substitusi resistensi insulin (seperti Indeks TyHbA1c, TG/HDL, Non-HDL, maupun SUA/HDL) tidak memberikan nilai prediktif diagnostik tambahan yang bermakna.

## Daftar Pustaka

::: {#refs}
:::

------------------------------------------------------------------------

## Lampiran

**Uji Normalitas Data Numerik**

Panel histogram dan uji Shapiro-Wilk di atas menampilkan distribusi data pada variabel numerik. Sebagian besar variabel klinis dan indeks kardiometabolik tidak berdistribusi normal (p < 0,05). Secara khusus, panel grafik menunjukkan bahwa meskipun telah dilakukan transformasi logaritmik (ln[CAC+1]), kurva skor CAC tidak sepenuhnya normal akibat tingginya proporsi pasien dengan skor nol mutlak (zero-inflated data). 

```{r}
#| label: fig-supp-s1
#| fig-cap: "**Supplementary S1. Panel Distribusi Histogram Data Numerik Kardiometabolik**"
#| fig-width: 10
#| fig-height: 8
#| warning: false
#| message: false

library(dplyr)
library(tidyr)
library(ggplot2)
library(flextable)

# 1. Menyiapkan Data & Merapikan Nama Variabel
df_numerik <- df_clean %>%
  mutate(
    `Skor_CAC_Asli` = Global_CAC_score,
    `Ln_CAC_Plus_1` = log(Global_CAC_score + 1)
  ) %>%
  select(where(is.numeric)) %>%
  # Menambahkan Durasi_DM_Bulan ke dalam daftar pengecualian
  select(-any_of(c(
    "Global_CAC_score", "log_CAC",           
    "No", "No. RM", "No_RM", "No__RM", "ID", 
    "Tahun",                                 
    "Alpha", "Kappa", "Sex_Mult",            
    "CKD_Status", "CAC_100_plus",
    "Durasi_DM_Bulan" 
  ))) %>%
  # Mengubah nama variabel (sistem ini sangat aman, hanya mengubah yang ada di data)
  rename(any_of(c(
    "Asam Urat" = "Asam-Urat",
    "Durasi DM (Tahun)" = "Durasi_DM",
    "Kolesterol HDL" = "HDL",
    "Kolesterol LDL" = "LDL",
    "Kolesterol Total" = "Kolesterol-Total",
    "Indeks TyHbA1c" = "TyHbA1c_Index",
    "Rasio SUA / HDL-C" = "SUA_HDL_Ratio",
    "Rasio TG / HDL-C" = "TG_HDL_Ratio",
    "Kolesterol Non-HDL" = "Non_HDL",
    "Skor CAC (Agatston)" = "Skor_CAC_Asli",
    "Skor CAC (ln[CAC+1])" = "Ln_CAC_Plus_1",
    "HbA1c" = "HbA1C"
  )))

# 2. FUNGSI AMAN SHAPIRO-WILK
uji_shapiro <- function(x) {
  x <- na.omit(x)
  if(length(x) >= 3 && length(x) <= 5000) {
    return(shapiro.test(x)$p.value)
  } else {
    return(NA)
  }
}

# 3. MEMBUAT TABEL NORMALITAS
tabel_normalitas <- df_numerik %>%
  summarise(across(everything(), uji_shapiro)) %>%
  pivot_longer(everything(), names_to = "Variabel", values_to = "P_Value") %>%
  mutate(
    Distribusi = ifelse(P_Value < 0.05, "Tidak Normal", "Normal"),
    P_Value_Format = ifelse(P_Value < 0.001, "< 0,001", format(round(P_Value, 3), decimal.mark=","))
  ) %>%
  select(Variabel, P_Value_Format, Distribusi) %>%
  arrange(Variabel) %>%
  flextable() %>%
  set_header_labels(
    Variabel = "Variabel Klinis / Indeks Metabolik", 
    P_Value_Format = "Nilai p (Shapiro-Wilk)", 
    Distribusi = "Distribusi"
  ) %>%
  bold(j = "Distribusi", i = ~ Distribusi == "Tidak Normal", bold = TRUE) %>%
  align(align = "center", part = "all") %>%
  autofit() %>%
  set_caption(caption = "Tabel S1. Hasil Uji Normalitas Shapiro-Wilk pada Variabel Numerik Klinis")

# 4. MEMBUAT PANEL HISTOGRAM
panel_histogram <- df_numerik %>%
  pivot_longer(everything(), names_to = "Variabel", values_to = "Nilai") %>%
  filter(!is.na(Nilai)) %>% 
  ggplot(aes(x = Nilai)) +
  geom_histogram(aes(y = after_stat(density)), fill = "#4A81BF", color = "white", bins = 20) +
  geom_density(color = "#D9534F", linewidth = 0.8) + 
  facet_wrap(~ Variabel, scales = "free") +
  theme_minimal(base_size = 12) +
  labs(
    x = "Nilai Distribusi",
    y = "Kepadatan (Density)"
  ) +
  theme(
    # Membuat judul di setiap grafik kecil lebih terbaca
    strip.text = element_text(face = "bold", size = 11, color = "black"),
    strip.background = element_rect(fill = "#F0F0F0", color = NA),
    panel.grid.minor = element_blank(),
    axis.text.y = element_blank(), 
    axis.ticks.y = element_blank()
  )

# 5. MENAMPILKAN HASIL SECARA BERURUTAN
tabel_normalitas
panel_histogram
```

**Analisis Karakteristik Klinis Tambahan Terhadap Indeks Kardiometabolik Novel**

Sebagai pelengkap analisis, evaluasi tabulasi silang juga dilakukan pada dua indeks kardiometabolik yakni Indeks TyHbA1c (@tbl-supp-tyhba1c) dan Rasio SUA/HDL-C (@tbl-supp-sua). Pada evaluasi Indeks TyHbA1c, variabel kontrol glikemik (HbA1c) sengaja dieksklusi dari model analisis untuk menghindari bias mengingat parameter tersebut merupakan komponen pembentuk rumus dasar indeks.

```{r}
#| label: tbl-supp-tyhba1c
#| tbl-cap: "**Karakteristik Klinis dan Profil Pengobatan Berdasarkan Kategori Indeks TyHbA1c**"

library(flextable)

med_ty <- median(df_clean$TyHbA1c_Index, na.rm = TRUE)

df_ty <- df_cac %>%
  mutate(
    TyHbA1c_Kat = factor(ifelse(TyHbA1c_Index > med_ty, "> Median (Tinggi)", "\u2264 Median (Rendah)"), 
                         levels = c("\u2264 Median (Rendah)", "> Median (Tinggi)"))
  ) %>%
  # Menambahkan Usia_Kat (Tetap Tanpa HbA1c_Kat)
  select(TyHbA1c_Kat, Usia_Kat, Gender, IMT_Kat, Hipertensi_Kat, Merokok, Riwayat_DM, Terapi_DM, Statin)

label_ty <- list(
  Usia_Kat ~ "Kategori Usia",
  Gender ~ "Jenis Kelamin",
  IMT_Kat ~ "Kategori IMT",
  Hipertensi_Kat ~ "Status Hipertensi",
  Merokok ~ "Riwayat Merokok",
  Riwayat_DM ~ "Durasi Diagnosis DM",
  Terapi_DM ~ "Modalitas Terapi DM",
  Statin ~ "Penggunaan Statin"
)

df_ty %>%
  tbl_summary(by = TyHbA1c_Kat, label = label_ty, missing = "no") %>%
  add_p() %>% 
  bold_p(t = 0.05) %>%
  modify_header(label = "**Faktor Klinis / Pengobatan**") %>%
  as_flex_table()
```

Hasil analisis komparasi silang pada Rasio Asam Urat/HDL-C (SUA/HDL) menunjukan adanya hubungan yang bermakna secara statistik terhadap status Indeks Massa Tubuh ($p = 0,039$) dan Usia ($p = 0,044$). Kelompok pasien dengan Rasio SUA/HDL yang tinggi (\> median) secara signifikan didominasi oleh pasien dengan status *obesitas* (81%). Temuan ini mendukung utilitas Rasio SUA/HDL sebagai penanda *surrogate* akumulasi lemak viseral dan profil sindrom metabolik. Selain itu, dominasi pasien lansia ($\ge$ 60 tahun) pada kelompok rasio SUA/HDL yang rendah (77%) secara tidak langsung turut mendukung hipotesis *metabolic burn-out*, di mana pasien dengan kalsifikasi lanjut cenderung mengalami penurunan massa otot atau malnutrisi kronis yang menurunkan laju produksi asam urat sirkulasi.

```{r}
#| label: tbl-supp-sua
#| tbl-cap: "**Karakteristik Klinis dan Profil Pengobatan Berdasarkan Kategori Rasio Asam Urat/HDL (SUA/HDL-C)**"

library(flextable)

med_sua <- median(df_clean$SUA_HDL_Ratio, na.rm = TRUE)

df_sua <- df_cac %>%
  mutate(
    SUA_HDL_Kat = factor(ifelse(SUA_HDL_Ratio > med_sua, "> Median (Tinggi)", "\u2264 Median (Rendah)"), 
                         levels = c("\u2264 Median (Rendah)", "> Median (Tinggi)"))
  ) %>%
  # Menambahkan Usia_Kat
  select(SUA_HDL_Kat, Usia_Kat, Gender, IMT_Kat, Hipertensi_Kat, Merokok, HbA1C_Kat, Riwayat_DM, Terapi_DM, Statin)

label_sua <- list(
  Usia_Kat ~ "Kategori Usia",
  Gender ~ "Jenis Kelamin",
  IMT_Kat ~ "Kategori IMT",
  Hipertensi_Kat ~ "Status Hipertensi",
  Merokok ~ "Riwayat Merokok",
  HbA1C_Kat ~ "Kontrol Glikemik (HbA1c)",
  Riwayat_DM ~ "Durasi Diagnosis DM",
  Terapi_DM ~ "Modalitas Terapi DM",
  Statin ~ "Penggunaan Statin"
)

df_sua %>%
  tbl_summary(by = SUA_HDL_Kat, label = label_sua, missing = "no") %>%
  add_p() %>% 
  bold_p(t = 0.05) %>%
  modify_header(label = "**Faktor Klinis / Pengobatan**") %>%
  as_flex_table()
```

**Eksplorasi Kurva ROC Indeks Kardiometabolik**

Pemetaan kurva *Receiver Operating Characteristic* (ROC) dilakukan secara bersamaan untuk keempat indeks turunan dalam memprediksi kerentanan plak (HRP). Analisis diskriminatif ini menegaskan bahwa tidak ada satu pun indeks kardiometabolik yang mencapai *Area Under the Curve* (AUC) yang memadai sebagai alat diagnostik utama (@fig-supp-roc).

```{r}
#| label: fig-supp-roc
#| fig-cap: "**Komparasi Kurva ROC 4 Indeks Kardiometabolik.** Kurva menunjukkan bahwa kemampuan diskriminasi mandiri dari TyHbA1c, TG/HDL, Non-HDL, dan SUA/HDL dalam memprediksi kerentanan plak (HRP) tergolong lemah pada kohort ini."
#| fig-width: 6
#| fig-height: 5

roc_ty <- roc(df_clean$HRP_Status, df_clean$TyHbA1c_Index, quiet=TRUE)
roc_tg <- roc(df_clean$HRP_Status, df_clean$TG_HDL_Ratio, quiet=TRUE)
roc_nhdl <- roc(df_clean$HRP_Status, df_clean$Non_HDL, quiet=TRUE)
roc_sua <- roc(df_clean$HRP_Status, df_clean$SUA_HDL_Ratio, quiet=TRUE)

plot(roc_ty, col="#D32F2F", lwd=2, main="Komparasi AUC Indeks Kardiometabolik terhadap HRP")
plot(roc_tg, col="#1976D2", lwd=2, add=TRUE)
plot(roc_nhdl, col="#388E3C", lwd=2, add=TRUE)
plot(roc_sua, col="#FBC02D", lwd=2, add=TRUE)

legend("bottomright", 
       legend=c(paste("TyHbA1c (AUC=", round(auc(roc_ty),2), ")"),
                paste("TG/HDL (AUC=", round(auc(roc_tg),2), ")"),
                paste("Non-HDL (AUC=", round(auc(roc_nhdl),2), ")"),
                paste("SUA/HDL (AUC=", round(auc(roc_sua),2), ")")),
       col=c("#D32F2F", "#1976D2", "#388E3C", "#FBC02D"), lwd=2, cex=0.9)
```

**Korelasi Komprehensif Seluruh Parameter Klinis dan Laboratorium**

Analisis korelasi linear dilakukan untuk mengevaluasi hubungan antara seluruh variabel kontinu (termasuk usia, IMT, parameter laboratorium standar, dan indeks kardiometabolik) terhadap nilai logaritmik absolut kalsifikasi koroner ($\ln[\text{CAC}+1]$). Hasil analisis menunjukan bahwa faktor usia merupakan satu-satunya variabel yang secara konsisten menunjukkan tren korelasi linear yang bermakna **positif** ($R = `r cor_usia$r`, p`r cor_usia$p`$). Sebaliknya, seluruh parameter lipid sirkulasi beserta indeks turunannya (termasuk Rasio TG/HDL dan Non-HDL) menunjukkan korelasi linear negatif yang tidak bermakna  (@fig-supp-scatter).

```{r}
#| label: fig-supp-scatter
#| fig-cap: "**Korelasi Parameter Klinis, Laboratorium, dan Indeks Kardiometabolik dengan Kalsifikasi Koroner**"
#| fig-width: 8.5
#| fig-height: 14
#| warning: false
#| message: false

library(ggplot2)
library(tidyr)
library(dplyr)

df_all_vars <- df_clean %>%
  mutate(
    log_CAC = log(Global_CAC_score + 1),
    TyHbA1c_Index = log(Trigliserida * HbA1C),
    TG_HDL_Ratio = Trigliserida / HDL,
    Non_HDL = `Kolesterol-Total` - HDL,
    SUA_HDL_Ratio = `Asam-Urat` / HDL
  ) %>%
  select(
    log_CAC, Age = Usia, BMI = IMT, HbA1c = HbA1C,
    Triglycerides = Trigliserida, `HDL-C` = HDL, `LDL-C` = LDL,
    `Total Cholesterol` = `Kolesterol-Total`, `Serum Uric Acid` = `Asam-Urat`,
    Creatinine = Kreatinin, `TyHbA1c Index` = TyHbA1c_Index,
    `TG / HDL-C Ratio` = TG_HDL_Ratio, `Non-HDL Cholesterol` = Non_HDL,
    `SUA / HDL-C Ratio` = SUA_HDL_Ratio
  ) %>%
  pivot_longer(
    cols = -log_CAC, names_to = "Variable", values_to = "Value"
  ) %>%
  mutate(Variable = factor(Variable, levels = c(
    "Age", "BMI", "HbA1c", "Triglycerides", "HDL-C", "LDL-C", 
    "Total Cholesterol", "Serum Uric Acid", "Creatinine",
    "TyHbA1c Index", "TG / HDL-C Ratio", "Non-HDL Cholesterol", "SUA / HDL-C Ratio"
  )))

# --- MENGHITUNG STATISTIK & MENENTUKAN FORMAT FONT ---
df_stats_all <- df_all_vars %>%
  filter(!is.na(Value) & !is.na(log_CAC)) %>%
  group_by(Variable) %>%
  summarise(
    rho = suppressWarnings(cor(Value, log_CAC, method = "spearman")),
    p_val = suppressWarnings(cor.test(Value, log_CAC, method = "spearman", exact = FALSE)$p.value),
    .groups = "drop"
  ) %>%
  mutate(
    p_text = ifelse(p_val < 0.001, "< 0.001", sprintf("= %.3f", p_val)),
    # Mengganti simbol dengan kata "rho"
    label_text = sprintf("rho = %.3f\np %s", rho, p_text),
    # Menentukan style huruf: tebal untuk p < 0.05, biasa untuk p >= 0.05
    font_style = ifelse(p_val < 0.05, "bold", "plain")
  )
# ---------------------------------------------------

p_comprehensive <- ggplot(df_all_vars, aes(x = Value, y = log_CAC)) +
  geom_point(size = 1.5, alpha = 0.5, color = "#2c3e50") +
  geom_smooth(method = "lm", se = TRUE, color = "#d35400", fill = "#edbb99", linewidth = 0.8) +
  # Memasukkan font_style ke dalam aesthetic (aes) agar menyesuaikan per grafik
  geom_text(data = df_stats_all, aes(x = Inf, y = Inf, label = label_text, fontface = font_style),
            hjust = 1.1, vjust = 1.2, size = 3.5, color = "black", inherit.aes = FALSE) +
  facet_wrap(~ Variable, scales = "free_x", ncol = 2) + 
  labs(x = "Parameter Value", y = "ln(Global CAC Score + 1)") +
  theme_bw(base_size = 11) +
  theme(
    strip.background = element_rect(fill = "#f2f3f4", color = "black"),
    strip.text = element_text(face = "bold", size = 10),
    panel.grid.minor = element_blank()
  )

# 4. Tampilkan plot ke dokumen
p_comprehensive
```

```{r}
#| label: fig-hidden-forest-plot
#| include: false
#| warning: false
#| message: false

library(ggplot2)
library(dplyr)

# 1. Tentukan variabel numerik yang akan dihitung Odds Ratio-nya (Univariat)
var_names <- c("Usia", "IMT", "HbA1C", "Trigliserida", "HDL", "LDL", 
               "Kolesterol-Total", "Asam-Urat", "Kreatinin", 
               "TyHbA1c_Index", "TG_HDL_Ratio", "Non_HDL", "SUA_HDL_Ratio")

var_labels <- c("Usia (Tahun)", "IMT (kg/m\u00b2)", "HbA1c (%)", "Trigliserida (mg/dL)", 
                "HDL-C (mg/dL)", "LDL-C (mg/dL)", "Kolesterol Total (mg/dL)", 
                "Asam Urat (mg/dL)", "Kreatinin (mg/dL)", "Indeks TyHbA1c", 
                "Rasio TG/HDL-C", "Kolesterol Non-HDL", "Rasio SUA/HDL-C")

# 2. Buat loop untuk regresi logistik univariat dan ekstraksi OR + 95% CI
forest_data <- data.frame()

for(i in 1:length(var_names)) {
  v <- var_names[i]
  lbl <- var_labels[i]
  
  # KUNCI PERBAIKAN: Gunakan as.formula dengan backtick (`) agar variabel berminus aman
  f <- as.formula(paste0("I(HRP_Status == 'HRP (+)') ~ `", v, "`"))
  
  # Jalankan model dengan referensi data langsung ke df_clean
  mod <- glm(f, data = df_clean, family = binomial)
  sm <- summary(mod)
  coefs <- sm$coefficients
  
  if(nrow(coefs) > 1) {
    est <- coefs[2, 1]
    se <- coefs[2, 2]
    pval <- coefs[2, 4]
    
    # Hitung Odds Ratio dan 95% CI
    or <- exp(est)
    lower <- exp(est - 1.96 * se)
    upper <- exp(est + 1.96 * se)
    
    forest_data <- rbind(forest_data, data.frame(
      Variable = lbl,
      OR = or,
      Lower = lower,
      Upper = upper,
      Pval = pval
    ))
  }
}

# 3. Format data untuk plotting
forest_data <- forest_data %>%
  mutate(
    # Beri warna khusus jika p-value < 0.05
    Signifikan = ifelse(Pval < 0.05, "Signifikan (p < 0.05)", "Tidak Signifikan"),
    # Urutkan variabel agar tampil rapi dari atas ke bawah
    Variable = factor(Variable, levels = rev(var_labels))
  )

# 4. Buat Forest Plot
p_forest <- ggplot(forest_data, aes(x = OR, y = Variable, color = Signifikan)) +
  # Garis vertikal di OR = 1 (Garis Null)
  geom_vline(xintercept = 1, linetype = "dashed", color = "gray50", linewidth = 0.8) +
  # Titik (OR) dan garis rentang (95% CI)
  geom_pointrange(aes(xmin = Lower, xmax = Upper), size = 0.8, fatten = 3) +
  # KUNCI PERBAIKAN: Format angka desimal biasa dan beri batas "zoom" (0.5 sampai 5)
  scale_x_log10(breaks = c(0.5, 1, 2, 3, 5)) +
  coord_cartesian(xlim = c(0.1, 10)) + # Memotong sumbu X di 0.5 - 5
  scale_color_manual(values = c("Signifikan (p < 0.05)" = "#c0392b", "Tidak Signifikan" = "#7f8c8d")) +
  labs(
    title = "Forest Plot Analisis Univariat: Prediktor High-Risk Plaque (HRP)",
    x = "Odds Ratio (Skala Logaritmik)",
    y = "Variabel Klinis & Kardiometabolik"
  ) +
  theme_bw(base_size = 11) +
  theme(
    legend.position = "bottom",
    legend.title = element_blank(),
    plot.title = element_text(face = "bold", hjust = 0.5)
  )

# 5. Ekspor plot
ggsave(filename = "Figure_4_Forest_Plot_Univariat.jpg", 
       plot = p_forest, 
       width = 9, height = 7, dpi = 300, device = "jpeg")
```

```{r}
#| include: false
# Pastikan paket yang dibutuhkan sudah aktif
library(ggplot2)
library(tidyr)
library(dplyr)

# Menyiapkan data (dengan label Bahasa Inggris)
df_cac_supp_eng <- df_clean %>%
  mutate(log_CAC = log(Global_CAC_score + 1)) %>%
  select(log_CAC, TyHbA1c_Index, TG_HDL_Ratio, Non_HDL, SUA_HDL_Ratio) %>%
  pivot_longer(
    cols = -log_CAC,
    names_to = "Indeks_Metabolik",
    values_to = "Nilai"
  ) %>%
  mutate(
    Indeks_Metabolik = case_when(
      Indeks_Metabolik == "TyHbA1c_Index" ~ "TyHbA1c Index",
      Indeks_Metabolik == "TG_HDL_Ratio" ~ "TG / HDL-C Ratio",
      Indeks_Metabolik == "Non_HDL" ~ "Non-HDL Cholesterol",
      Indeks_Metabolik == "SUA_HDL_Ratio" ~ "SUA / HDL-C Ratio"
    ),
    Indeks_Metabolik = factor(Indeks_Metabolik, levels = c(
      "TyHbA1c Index", "TG / HDL-C Ratio", "Non-HDL Cholesterol", "SUA / HDL-C Ratio"
    ))
  )

# Menghitung statistik (R, R2, p-value)
df_stats_eng <- df_cac_supp_eng %>%
  group_by(Indeks_Metabolik) %>%
  summarise(
    r = cor(Nilai, log_CAC, method = "pearson"),
    r2 = r^2,
    p_val = cor.test(Nilai, log_CAC, method = "pearson")$p.value,
    .groups = "drop"
  ) %>%
  mutate(
    p_text = ifelse(p_val < 0.001, "< 0.001", sprintf("= %.3f", p_val)),
    label_text = sprintf("R = %.3f\nR\u00b2 = %.3f\np %s", r, r2, p_text)
  )

# Menyimpan plot ke dalam objek p_scatter
p_scatter <- ggplot(df_cac_supp_eng, aes(x = Nilai, y = log_CAC)) +
  geom_point(size = 2, alpha = 0.6, color = "#2c3e50") +
  geom_smooth(method = "lm", se = TRUE, color = "#d35400", fill = "#edbb99", linewidth = 1) +
  geom_text(data = df_stats_eng, aes(x = Inf, y = Inf, label = label_text),
            hjust = 1.1, vjust = 1.2, size = 3.5, fontface = "italic", color = "black", inherit.aes = FALSE) +
  facet_wrap(~ Indeks_Metabolik, scales = "free_x", ncol = 2) +
  labs(
    x = "Cardiometabolic Index Value",
    y = "ln(Global CAC Score + 1)"
  ) +
  theme_bw(base_size = 12) +
  theme(
    strip.background = element_rect(fill = "#f8f9f9", color = "black"),
    strip.text = element_text(face = "bold", size = 11),
    panel.grid.minor = element_blank()
  )

# Mencetak dengan ggsave (.jpg resolusi 300 dpi)
ggsave(filename = "Figure_2_Scatter_CAC.jpg", plot = p_scatter, width = 9, height = 7, dpi = 300, device = "jpeg")

cat("File Figure_2_Scatter_CAC.jpg berhasil disimpan!\n")
```

```{r}
#| label: fig-hidden-strobe
#| include: false
#| warning: false
#| message: false

library(ggplot2)

# 1. Siapkan teks untuk masing-masing kotak 
# (Silakan ganti angka n=100 dan n=48 dengan data taksiran nyata Dokter)
df_box <- data.frame(
  x = c(0.4, 0.85, 0.4),
  y = c(0.8, 0.5, 0.2),
  label = c("Pasien DM yang menjalani CTCA\n(n = 100)", 
            "Eksklusi (n = 48):\n- Data lab/rekam medis tidak lengkap (n = 20)\n- Riwayat CABG (n = 15)\n- ESRD (n = 13)", 
            "Subjek memenuhi kriteria inklusi\ndan dianalisis (n = 52)")
)

# 2. Gambar panah dan kotak label (Flowchart)
p_flow <- ggplot() +
  # Garis vertikal utama (ke kotak bawah)
  annotate("segment", x = 0.4, y = 0.75, xend = 0.4, yend = 0.25, 
           arrow = arrow(length = unit(0.3, "cm")), linewidth = 0.8, color = "gray30") +
  # Garis horizontal cabang (ke kotak eksklusi di kanan)
  annotate("segment", x = 0.4, y = 0.5, xend = 0.58, yend = 0.5, 
           arrow = arrow(length = unit(0.3, "cm")), linewidth = 0.8, color = "gray30") +
  # Tambahkan kotak teks
  geom_label(data = df_box, aes(x = x, y = y, label = label), 
             size = 4, fill = "#f8f9f9", fontface = "bold", color = "#2c3e50",
             label.padding = unit(0.8, "lines"), label.r = unit(0.2, "lines")) +
  # Bersihkan semua elemen background (karena ini diagram, bukan grafik data)
  theme_void() +
  coord_cartesian(xlim = c(0.1, 1.2), ylim = c(0, 1))

# 3. Simpan diam-diam ke dalam format JPEG
ggsave(filename = "Figure_5_STROBE_Flowchart.jpg", 
       plot = p_flow, 
       width = 7, height = 5, dpi = 300, device = "jpeg")
```

```{r}
#| label: ekspor-data-spss
#| include: false
#| warning: false

library(haven)
library(dplyr)

# 1. Membersihkan nama kolom DAN tipe data untuk SPSS
df_spss <- df_clean %>%
  # Meratakan/mengubah kolom bertipe 'list' menjadi teks biasa ('character')
  mutate(across(where(is.list), ~ sapply(.x, toString))) %>%
  # Mengubah semua spasi, titik, dan tanda baca pada nama kolom menjadi underscore
  rename_with(~ gsub("[^A-Za-z0-9]", "_", .x))

# 2. Mengekspor dataframe SPSS
write_sav(df_spss, "Data_CTCA_Kardiometabolik.sav")

# 3. Membuat file CSV yang juga bebas dari 'list'
df_csv <- df_clean %>%
  mutate(across(where(is.list), ~ sapply(.x, toString)))

write.csv(df_csv, "Data_CTCA_Kardiometabolik.csv", row.names = FALSE)
```


```{r}
#| label: fig-summary-slide
#| fig-width: 13.33
#| fig-height: 7.5
#| warning: false
#| message: false

library(ggplot2)
library(dplyr)

# 1. Menyiapkan Data Tabel 1 & 2 (Ditambahkan Median & Q1-Q3 sesuai Naskah)
df_summary <- data.frame(
  Parameter = factor(
    rep(c("Skor CAC\n(Agatston)", "Usia\n(Tahun)"), each = 2), 
    levels = c("Skor CAC\n(Agatston)", "Usia\n(Tahun)")
  ),
  HRP_Status = rep(c("HRP (-)", "HRP (+)"), 2),
  Nilai = c(12, 557, 57, 64),
  # Data di bawah diambil persis dari rentang Q1-Q3 di Tabel 1 & 2 naskah
  Teks_Nilai = c("12 (0-18)", "557 (219-1.014)", "57 (48-63)", "64 (58-69)") 
) %>%
  group_by(Parameter) %>%
  mutate(
    Max_Nilai = max(Nilai),
    Plot_X = Nilai / Max_Nilai,
    Plot_X = ifelse(HRP_Status == "HRP (-)", -Plot_X, Plot_X),
    # Jarak teks dijauhkan sedikit karena string (Q1-Q3) memakan ruang lebih panjang
    Text_X = ifelse(HRP_Status == "HRP (-)", Plot_X - 0.08, Plot_X + 0.08),
    Hjust = ifelse(HRP_Status == "HRP (-)", 1, 0)
  ) %>% ungroup()

# 2. Menyiapkan Data untuk Anotasi P-Value
df_pvals <- data.frame(
  Parameter = factor(c("Usia\n(Tahun)", "Skor CAC\n(Agatston)"), 
                     levels = c("Skor CAC\n(Agatston)", "Usia\n(Tahun)")),
  Label_P = c("Signifikan, p = 0,010", "Signifikan, p < 0,001"),
  X_Pos = c(1.0, 1.0), 
  Y_Pos = c(2.35, 1.35)  
)

# 3. Membuat Grafik Infografis Slide yang Disempurnakan
p_infographic <- ggplot(df_summary, aes(y = Parameter)) +
  
  geom_vline(xintercept = 0, color = "black", linewidth = 1.2) +
  
  geom_col(aes(x = Plot_X, fill = HRP_Status), width = 0.45, color = "black", linewidth = 0.8) +
  
  # Ukuran teks angka (Q1-Q3) sedikit disesuaikan agar rapi
  geom_text(aes(x = Text_X, label = Teks_Nilai, hjust = Hjust), size = 6.5, fontface = "bold", color = "black") +
  
  # KUNCI 3: Label Parameter diturunkan letaknya (-0.38) agar berada di bawah balok, tidak menutupi garis 0
  geom_label(aes(x = 0, y = as.numeric(Parameter) - 0.38, label = Parameter), size = 6, fontface = "bold", color = "black",
             fill = "white", label.size = 0, label.padding = unit(0.5, "lines")) +
             
  # Label HRP (-) dan (+) diturunkan sedikit koordinat Y-nya
  annotate("text", x = -0.5, y = 2.45, label = "HRP (-)", size = 9, fontface = "bold", color = "#7F8C8D") +
  annotate("text", x = 0.5, y = 2.45, label = "HRP (+)", size = 9, fontface = "bold", color = "#C0392B") +

  geom_label(data = df_pvals, aes(x = X_Pos, y = Y_Pos, label = Label_P),
             size = 6, fontface = "bold", fill = "#ECF0F1", color = "black",
             label.size = 0.8, label.padding = unit(0.6, "lines"), hjust = 0.5) +
             
  geom_curve(data = df_pvals, aes(x = X_Pos - 0.2, y = Y_Pos - 0.05, 
                                  xend = 0.5, yend = as.numeric(Parameter) + 0.15),
             curvature = 0.2, linewidth = 0.8, color = "black", 
             arrow = arrow(length = unit(0.02, "npc"), type = "closed")) +

  scale_fill_manual(values = c("HRP (-)" = "#CCCCCC", "HRP (+)" = "#E64B35")) +
  
  # Rentang X diperlebar ke -2.0 s/d 2.0 agar teks (Q1-Q3) yang panjang tidak keluar frame
  scale_x_continuous(limits = c(-2.0, 2.0)) + 
  scale_y_discrete(expand = expansion(mult = c(0.2, 0.3))) + 
  
  # KUNCI 1: Clip = "off" mengizinkan elemen grafis digambar keluar dari batas kanvas (mencegah terpotong)
  coord_cartesian(clip = "off") +
  
  labs(
    title = "Temuan Utama: Dominansi Usia dalam Kerentanan Plak",
    caption = "Kesimpulan: Usia merupakan satu-satunya determinan klinis yang mencapai signifikansi statistik,\nmembuktikan bahwa aterosklerosis adalah patologi yang sangat bergantung pada waktu (time-dependent)."
  ) +
  
  theme_void(base_size = 20) + 
  theme(
    legend.position = "none",
    plot.title = element_text(face = "bold", hjust = 0.5, size = 28, margin = margin(t = 10, b = 20)),
    plot.caption = element_text(size = 18, hjust = 0.5, face = "italic", color = "black",
                                margin = margin(t = 30), lineheight = 1.3),
    plot.background = element_rect(fill = "white", color = NA)
  )

p_infographic

ggsave("Grafik/Slide_Infografis_Kesimpulan_PPT.png", plot = p_infographic, width = 16, height = 9, dpi = 300)
```


```{r}
#| label: fig-box-klinis
#| fig-width: 16
#| fig-height: 9
#| warning: false
#| message: false

library(ggplot2)
library(dplyr)
library(tidyr)
library(patchwork)

# 1. Menyeleksi 5 Variabel Kelompok 1 (MENGGUNAKAN NAMA KOLOM ASLI)
df_group1 <- df_clean %>%
  select(HRP_Status, Usia, IMT, Hemoglobin, Global_CAC_score, Kreatinin) %>%
  rename(
    `Usia (Tahun)` = Usia,
    `IMT (kg/m2)` = IMT,
    `Hemoglobin (g/dL)` = Hemoglobin,
    `Skor CAC (Agatston)` = Global_CAC_score,
    `Serum Kreatinin (mg/dL)` = Kreatinin
  ) %>%
  pivot_longer(cols = -HRP_Status, names_to = "Variabel", values_to = "Nilai") %>%
  filter(!is.na(Nilai))

df_overall <- df_group1 %>% mutate(HRP_Status = "Keseluruhan")
df_group1 <- bind_rows(df_overall, df_group1) %>%
  mutate(HRP_Status = factor(HRP_Status, levels = c("Keseluruhan", "HRP (-)", "HRP (+)")))

buat_plot <- function(nama_var) {
  df_sub <- df_group1 %>% filter(Variabel == nama_var)
  
  df_pval <- df_sub %>% filter(HRP_Status != "Keseluruhan") %>%
    summarise(p_val = wilcox.test(Nilai ~ HRP_Status)$p.value) %>%
    mutate(p_text = ifelse(p_val < 0.001, "p < 0,001", paste0("p = ", format(round(p_val, 3), decimal.mark=","))),
           font_style = ifelse(p_val < 0.05, "bold", "plain"))
           
  df_med <- df_sub %>% group_by(HRP_Status) %>%
    summarise(Q1 = quantile(Nilai, 0.25, na.rm=TRUE), Median = median(Nilai, na.rm=TRUE), Q3 = quantile(Nilai, 0.75, na.rm=TRUE)) %>%
    mutate(label_med = paste0(format(round(Median,1), decimal.mark=",", trim=TRUE), "(", 
                              format(round(Q1,1), decimal.mark=",", trim=TRUE), "-", 
                              format(round(Q3,1), decimal.mark=",", trim=TRUE), ")"))
                              
  ggplot(df_sub, aes(x = HRP_Status, y = Nilai, fill = HRP_Status)) +
    geom_jitter(aes(color = HRP_Status), width = 0.15, alpha = 0.4, size = 2.5, shape = 16) +
    geom_boxplot(alpha = 0.8, color = "black", linewidth = 0.8, outlier.shape = NA, width = 0.45) +
    geom_text(data = df_pval, aes(x = 2.5, y = Inf, label = p_text, fontface = font_style), 
              vjust = 1.2, size = 6, color = "black", inherit.aes = FALSE) +
    geom_label(data = df_med, aes(x = HRP_Status, y = -Inf, label = label_med), 
               vjust = -0.5, size = 4, fontface = "bold", color = "black", fill = alpha("white", 0.7), label.size = 0, inherit.aes = FALSE) +
    scale_fill_manual(values = c("Keseluruhan" = "#FFFFFF", "HRP (-)" = "#CCCCCC", "HRP (+)" = "#E64B35")) +
    scale_color_manual(values = c("Keseluruhan" = "#95A5A6", "HRP (-)" = "#7F8C8D", "HRP (+)" = "#C0392B")) +
    scale_y_continuous(expand = expansion(mult = c(0.12, 0.15))) + 
    labs(title = nama_var, x = "", y = "") +
    theme_classic(base_size = 18) + 
    theme(legend.position = "none", plot.title = element_text(face = "bold", hjust = 0.5, size = 18),
          axis.line = element_line(color = "black", linewidth = 1), axis.text.x = element_text(face = "bold", size = 12, color = "black"),
          axis.text.y = element_text(size = 14, color = "black"))
}

p_usia <- buat_plot("Usia (Tahun)") + theme(plot.title = element_text(size = 22))
p_imt <- buat_plot("IMT (kg/m2)")
p_hb <- buat_plot("Hemoglobin (g/dL)")
p_cac <- buat_plot("Skor CAC (Agatston)")
p_kre <- buat_plot("Serum Kreatinin (mg/dL)")

layout_asimetris <- p_usia | (p_imt + p_hb + p_cac + p_kre + plot_layout(ncol = 2))

final_plot_1 <- layout_asimetris + plot_layout(widths = c(1, 2)) + 
  plot_annotation(title = "Karakteristik Klinis Dasar Berdasarkan Status HRP", 
                  theme = theme(plot.title = element_text(face = "bold", size = 26, hjust = 0.5)))

final_plot_1

ggsave("Grafik/Presentasi_Boxplot_Grup1_Asimetris.png", plot = final_plot_1, width = 16, height = 9, dpi = 300)
```

```{r}
#| label: fig-box-lab
#| fig-width: 16
#| fig-height: 9
#| warning: false
#| message: false

library(ggplot2)
library(dplyr)
library(tidyr)

# 1. Menyeleksi 6 Variabel Kelompok 2 (MENGGUNAKAN NAMA KOLOM ASLI dengan Backtick)
df_group2 <- df_clean %>%
  select(HRP_Status, HbA1C, `Kolesterol-Total`, LDL, HDL, Trigliserida, `Asam-Urat`) %>%
  rename(
    `HbA1c (%)` = HbA1C,
    `Kolesterol Total (mg/dL)` = `Kolesterol-Total`,
    `Kolesterol LDL (mg/dL)` = LDL,
    `Kolesterol HDL (mg/dL)` = HDL,
    `Trigliserida (mg/dL)` = Trigliserida,
    `Asam Urat Serum (mg/dL)` = `Asam-Urat`
  ) %>%
  pivot_longer(cols = -HRP_Status, names_to = "Variabel", values_to = "Nilai") %>%
  filter(!is.na(Nilai))

df_overall2 <- df_group2 %>% mutate(HRP_Status = "Keseluruhan")
df_group2 <- bind_rows(df_overall2, df_group2) %>%
  mutate(HRP_Status = factor(HRP_Status, levels = c("Keseluruhan", "HRP (-)", "HRP (+)")))

df_pvals2 <- df_group2 %>% filter(HRP_Status != "Keseluruhan") %>% group_by(Variabel) %>%
  summarise(p_val = wilcox.test(Nilai ~ HRP_Status)$p.value, .groups = "drop") %>%
  mutate(p_text = ifelse(p_val < 0.001, "p < 0,001", paste0("p = ", format(round(p_val, 3), decimal.mark=","))),
         font_style = ifelse(p_val < 0.05, "bold", "plain"))

df_medians2 <- df_group2 %>% group_by(Variabel, HRP_Status) %>%
  summarise(Q1 = quantile(Nilai, 0.25, na.rm=TRUE), Median = median(Nilai, na.rm=TRUE), Q3 = quantile(Nilai, 0.75, na.rm=TRUE), .groups = "drop") %>%
  mutate(label_med = paste0(format(round(Median,1), decimal.mark=",", trim=TRUE), "(", 
                            format(round(Q1,1), decimal.mark=",", trim=TRUE), "-", 
                            format(round(Q3,1), decimal.mark=",", trim=TRUE), ")"))

final_plot_2 <- ggplot(df_group2, aes(x = HRP_Status, y = Nilai, fill = HRP_Status)) +
  geom_jitter(aes(color = HRP_Status), width = 0.15, alpha = 0.4, size = 2.5, shape = 16) +
  geom_boxplot(alpha = 0.8, color = "black", linewidth = 0.8, outlier.shape = NA, width = 0.45) +
  geom_text(data = df_pvals2, aes(x = 2.5, y = Inf, label = p_text, fontface = font_style), vjust = 1.2, size = 6.5, color = "black", inherit.aes = FALSE) +
  geom_label(data = df_medians2, aes(x = HRP_Status, y = -Inf, label = label_med), vjust = -0.5, size = 4.2, fontface = "bold", color = "black", fill = alpha("white", 0.7), label.size = 0, inherit.aes = FALSE) +
  facet_wrap(~ Variabel, scales = "free_y", ncol = 3) + 
  scale_fill_manual(values = c("Keseluruhan" = "#FFFFFF", "HRP (-)" = "#CCCCCC", "HRP (+)" = "#E64B35")) +
  scale_color_manual(values = c("Keseluruhan" = "#95A5A6", "HRP (-)" = "#7F8C8D", "HRP (+)" = "#C0392B")) +
  scale_y_continuous(expand = expansion(mult = c(0.12, 0.15))) + 
  labs(title = "Profil Laboratorium Glikemik, Lipid & Asam Urat", x = "", y = "Nilai Parameter") +
  theme_classic(base_size = 20) + 
  theme(legend.position = "none", strip.background = element_blank(), strip.text = element_text(face = "bold", size = 18, color = "black"),
        axis.line = element_line(color = "black", linewidth = 1), axis.text.x = element_text(face = "bold", size = 14, color = "black"),
        plot.title = element_text(face = "bold", hjust = 0.5, size = 26, margin = margin(b = 20)))

final_plot_2

ggsave("Grafik/Presentasi_Boxplot_Grup2_Grid.png", plot = final_plot_2, width = 16, height = 9, dpi = 300)
```

```{r}
#| label: fig-box-metabolik
#| fig-width: 16
#| fig-height: 9
#| warning: false
#| message: false

library(ggplot2)
library(dplyr)
library(tidyr)

# 1. Menyeleksi 4 Variabel Kelompok 3
df_group3 <- df_clean %>%
  select(HRP_Status, TyHbA1c_Index, TG_HDL_Ratio, Non_HDL, SUA_HDL_Ratio) %>%
  rename(
    `Indeks TyHbA1c` = TyHbA1c_Index,
    `Rasio TG / HDL-C` = TG_HDL_Ratio,
    `Kolesterol Non-HDL (mg/dL)` = Non_HDL,
    `Rasio SUA / HDL-C` = SUA_HDL_Ratio
  ) %>%
  pivot_longer(cols = -HRP_Status, names_to = "Variabel", values_to = "Nilai") %>%
  filter(!is.na(Nilai))

df_overall3 <- df_group3 %>% mutate(HRP_Status = "Keseluruhan")
df_group3 <- bind_rows(df_overall3, df_group3) %>%
  mutate(HRP_Status = factor(HRP_Status, levels = c("Keseluruhan", "HRP (-)", "HRP (+)")))

df_pvals3 <- df_group3 %>% filter(HRP_Status != "Keseluruhan") %>% group_by(Variabel) %>%
  summarise(p_val = wilcox.test(Nilai ~ HRP_Status)$p.value, .groups = "drop") %>%
  mutate(p_text = ifelse(p_val < 0.001, "p < 0,001", paste0("p = ", format(round(p_val, 3), decimal.mark=","))),
         font_style = ifelse(p_val < 0.05, "bold", "plain"))

df_medians3 <- df_group3 %>% group_by(Variabel, HRP_Status) %>%
  summarise(Q1 = quantile(Nilai, 0.25, na.rm=TRUE), Median = median(Nilai, na.rm=TRUE), Q3 = quantile(Nilai, 0.75, na.rm=TRUE), .groups = "drop") %>%
  mutate(label_med = paste0(format(round(Median,1), decimal.mark=",", trim=TRUE), "(", 
                            format(round(Q1,1), decimal.mark=",", trim=TRUE), "-", 
                            format(round(Q3,1), decimal.mark=",", trim=TRUE), ")"))

final_plot_3 <- ggplot(df_group3, aes(x = HRP_Status, y = Nilai, fill = HRP_Status)) +
  geom_jitter(aes(color = HRP_Status), width = 0.15, alpha = 0.4, size = 2.5, shape = 16) +
  geom_boxplot(alpha = 0.8, color = "black", linewidth = 0.8, outlier.shape = NA, width = 0.45) +
  geom_text(data = df_pvals3, aes(x = 2.5, y = Inf, label = p_text, fontface = font_style), vjust = 1.2, size = 7.5, color = "black", inherit.aes = FALSE) +
  geom_label(data = df_medians3, aes(x = HRP_Status, y = -Inf, label = label_med), vjust = -0.5, size = 5, fontface = "bold", color = "black", fill = alpha("white", 0.7), label.size = 0, inherit.aes = FALSE) +
  facet_wrap(~ Variabel, scales = "free_y", ncol = 2) + # KUNCI: 2 Kolom (2x2 Grid)
  scale_fill_manual(values = c("Keseluruhan" = "#FFFFFF", "HRP (-)" = "#CCCCCC", "HRP (+)" = "#E64B35")) +
  scale_color_manual(values = c("Keseluruhan" = "#95A5A6", "HRP (-)" = "#7F8C8D", "HRP (+)" = "#C0392B")) +
  scale_y_continuous(expand = expansion(mult = c(0.12, 0.15))) + 
  labs(title = "Indeks Kardiometabolik Turunan (Surrogate Markers)", x = "", y = "Nilai Parameter") +
  theme_classic(base_size = 20) + 
  theme(legend.position = "none", strip.background = element_blank(), strip.text = element_text(face = "bold", size = 18, color = "black"),
        axis.line = element_line(color = "black", linewidth = 1), axis.text.x = element_text(face = "bold", size = 14, color = "black"),
        plot.title = element_text(face = "bold", hjust = 0.5, size = 26, margin = margin(b = 20)))

final_plot_3
ggsave("Grafik/Presentasi_Boxplot_Grup3_Turunan.png", plot = final_plot_3, width = 16, height = 9, dpi = 300)
```


```{r}
#| label: fig-cat-diverging
#| fig-width: 16
#| fig-height: 9
#| warning: false
#| message: false

library(ggplot2)
library(dplyr)
library(tidyr)
library(scales) 

# 1. Menyiapkan Data Mentah (Memasukkan ke-7 Variabel Kategorik)
df_cat_raw <- df_clean %>%
  select(HRP_Status, Gender, Hipertensi_Kat, Merokok, CKD_Status, Statin, Terapi_DM, Riwayat_DM) %>%
  mutate(
    # Pemetaan Label agar sesuai dengan naskah
    Gender = factor(Gender, levels = c("L", "P"), labels = c("Laki-laki", "Perempuan")),
    Statin = factor(Statin, levels = c("No-Statin", "On-Statin"), labels = c("Tanpa Statin", "Pengguna Statin")),
    Merokok = factor(Merokok, labels = c("Tidak Merokok", "Merokok")),
    CKD_Status = factor(CKD_Status, levels = c(0, 1), labels = c("Normal/PGK \u2264 2", "PGK \u2265 3"))
  ) %>%
  rename(
    `Jenis Kelamin` = Gender,
    `Hipertensi` = Hipertensi_Kat,
    `Fungsi Ginjal` = CKD_Status,
    `Penggunaan Statin` = Statin,
    `Modalitas Terapi DM` = Terapi_DM,
    `Durasi Diagnosis DM` = Riwayat_DM
  ) %>%
  pivot_longer(cols = -HRP_Status, names_to = "Kategori", values_to = "Grup") %>%
  filter(!is.na(Grup)) %>%
  mutate(
    # KUNCI: Mengunci urutan agar sesuai permintaan (1-4 di Atas, 5-7 di Bawah)
    Kategori = factor(Kategori, levels = c(
      "Jenis Kelamin", "Hipertensi", "Merokok", "Fungsi Ginjal", 
      "Penggunaan Statin", "Modalitas Terapi DM", "Durasi Diagnosis DM"
    ))
  )

# 2. Menghitung P-value (Chi-Square/Fisher) untuk di Atas Grafik
df_pvals_cat <- df_cat_raw %>%
  group_by(Kategori) %>%
  summarise(
    p_val = suppressWarnings(chisq.test(table(Grup, HRP_Status))$p.value),
    # Titik kordinat X diletakkan persis di tengah (bisa untuk 2 atau 3 balok)
    x_pos = (n_distinct(Grup) + 1) / 2, 
    .groups = "drop"
  ) %>%
  mutate(
    p_text = ifelse(p_val < 0.001, "p < 0,001", paste0("p = ", format(round(p_val, 3), decimal.mark=","))),
    font_style = ifelse(p_val < 0.05, "bold", "plain")
  )

# 3. Menghitung Proporsi untuk Diverging Bar
df_cat_vis <- df_cat_raw %>%
  group_by(Kategori, Grup, HRP_Status) %>%
  summarise(Jumlah = n(), .groups = "drop") %>%
  group_by(Kategori, Grup) %>%
  mutate(
    Persentase = Jumlah / sum(Jumlah),
    # HRP (+) diubah menjadi negatif agar balok mengarah ke bawah
    Persentase_Plot = ifelse(HRP_Status == "HRP (+)", -Persentase, Persentase)
  )

# 4. Membuat Diverging Bar Chart (7 Panel)
p_diverging <- ggplot(df_cat_vis, aes(x = Grup, y = Persentase_Plot, fill = HRP_Status)) +
  
  geom_hline(yintercept = 0, color = "black", linewidth = 1.2) +
  
  geom_col(color = "black", width = 0.6, linewidth = 0.8) +
  
  # Angka persentase (Font dikecilkan sedikit menjadi 5 agar muat 4 kolom)
  geom_text(aes(y = Persentase_Plot / 2, label = percent(abs(Persentase_Plot), accuracy = 1)), 
            color = "black", fontface = "bold", size = 5) +
            
  # Teks P-value
  geom_text(data = df_pvals_cat, aes(x = x_pos, y = 1.15, label = p_text, fontface = font_style), 
            size = 5.5, color = "black", inherit.aes = FALSE) +
            
  # KUNCI LAYOUT: ncol = 4 akan otomatis membuat 4 kolom di atas dan sisanya (3) di bawah
  facet_wrap(~ Kategori, scales = "free_x", ncol = 4) +
  
  scale_fill_manual(values = c("HRP (-)" = "#CCCCCC", "HRP (+)" = "#E64B35")) +
  
  # Pemotongan kata diturunkan ke 10 karakter agar lebih ramping
  scale_x_discrete(labels = label_wrap(10)) +
  
  scale_y_continuous(
    labels = function(x) percent(abs(x), accuracy = 1), 
    limits = c(-1, 1.3), # Ditinggikan sedikit agar P-value di judul tidak terpotong
    breaks = seq(-1, 1, by = 0.5) 
  ) +
  
  labs(
    title = "Proporsi Kerentanan Plak (HRP) Berdasarkan Karakteristik Klinis",
    x = "",
    y = "HRP (+) \u2193   Persentase Proporsi (%)   \u2191 HRP (-)" 
  ) +
  
  theme_classic(base_size = 18) +
  theme(
    legend.position = "bottom",
    legend.title = element_blank(),
    legend.text = element_text(face = "bold", size = 16),
    strip.background = element_blank(),
    strip.text = element_text(face = "bold", size = 16, color = "black"),
    
    axis.line.x = element_blank(),
    axis.ticks.x = element_blank(),
    axis.line.y = element_line(color = "black", linewidth = 1),
    axis.ticks.y = element_line(color = "black", linewidth = 1),
    
    # Ukuran teks X disesuaikan
    axis.text.x = element_text(face = "bold", size = 12, color = "black", margin = margin(t = 5), lineheight = 0.8),
    axis.text.y = element_text(size = 12, color = "black"),
    axis.title.y = element_text(face = "bold", size = 16, margin = margin(r = 10)),
    panel.spacing = unit(1.2, "lines"),
    plot.title = element_text(face = "bold", hjust = 0.5, size = 26, margin = margin(b = 20))
  )

p_diverging

ggsave("Grafik/Grafik_Kategorik_Diverging_7Panel_PPT.png", plot = p_diverging, width = 16, height = 9, dpi = 300)
```

```{r}
#| label: fig-cac-sig
#| fig-width: 12
#| fig-height: 7
#| warning: false
#| message: false

library(ggplot2)
library(dplyr)
library(tidyr)

# 1. Menyiapkan Variabel Signifikan
df_sig <- df_clean %>%
  mutate(
    `Status High-Risk Plaque` = HRP_Status,
    `Kategori Usia` = factor(ifelse(Usia >= 60, "\u2265 60 Tahun", "< 60 Tahun"), levels = c("< 60 Tahun", "\u2265 60 Tahun"))
  ) %>%
  select(Global_CAC_score, `Status High-Risk Plaque`, `Kategori Usia`) %>%
  pivot_longer(cols = -Global_CAC_score, names_to = "Kategori", values_to = "Grup") %>%
  filter(!is.na(Grup)) %>%
  mutate(Kategori = factor(Kategori, levels = c("Status High-Risk Plaque", "Kategori Usia")))

# 2. Menghitung Nilai Med(Q1-Q3) dengan Spasi yang Diperbaiki (trim = TRUE)
df_stats_sig <- df_sig %>%
  group_by(Kategori, Grup) %>%
  summarise(
    Q1 = quantile(Global_CAC_score, 0.25, na.rm=TRUE),
    Median = median(Global_CAC_score, na.rm=TRUE),
    Q3 = quantile(Global_CAC_score, 0.75, na.rm=TRUE),
    .groups = "drop"
  ) %>%
  mutate(
    # KUNCI PERBAIKAN: Menambahkan trim = TRUE untuk membuang padding spasi otomatis dari R
    med_str = format(round(Median, 0), big.mark=".", trim = TRUE),
    q1_str  = format(round(Q1, 0), big.mark=".", trim = TRUE),
    q3_str  = format(round(Q3, 0), big.mark=".", trim = TRUE),
    label_med = paste0(med_str, " (", q1_str, "-", q3_str, ")")
  )

df_p_sig <- df_sig %>% group_by(Kategori) %>%
  summarise(p_val = wilcox.test(Global_CAC_score ~ Grup)$p.value, .groups = "drop") %>%
  mutate(p_text = ifelse(p_val < 0.001, "p < 0,001", paste0("p = ", format(round(p_val, 3), decimal.mark=","))))

# 3. Plotting dengan Color Mapping Kustom
p_sig <- ggplot(df_sig, aes(x = Grup, y = Global_CAC_score, fill = Grup)) +
  
  geom_jitter(aes(color = Grup), width = 0.15, alpha = 0.5, size = 3, shape = 16) +
  geom_boxplot(color = "black", linewidth = 0.8, outlier.shape = NA, width = 0.5, alpha = 0.8) +
  
  geom_text(data = df_p_sig, aes(x = 1.5, y = 3000, label = p_text), fontface = "bold", size = 7, color = "#C0392B", inherit.aes = FALSE) +
  geom_label(data = df_stats_sig, aes(x = Grup, y = 0, label = label_med), vjust = 1.2, size = 5, fontface = "bold", color = "black", fill = "white", label.size = 0, inherit.aes = FALSE) +
  
  facet_wrap(~ Kategori, scales = "free_x", ncol = 2) +
  
  scale_fill_manual(values = c(
    "HRP (-)" = "#CCCCCC",           
    "HRP (+)" = "#E64B35",           
    "< 60 Tahun" = "#4DBBD5",        
    "\u2265 60 Tahun" = "#F39C12"    
  )) +
  scale_color_manual(values = c(
    "HRP (-)" = "#7F8C8D", 
    "HRP (+)" = "#C0392B", 
    "< 60 Tahun" = "#2980B9", 
    "\u2265 60 Tahun" = "#D35400"
  )) +
  
  scale_y_continuous(trans = "log1p", breaks = c(0, 10, 100, 500, 1000, 2000), expand = expansion(mult = c(0.15, 0.15))) +
  
  labs(title = "Determinan Klinis Utama Keparahan Kalsifikasi Koroner (CAC)", y = "Skor CAC Absolut (Agatston)", x = "") +
  theme_classic(base_size = 20) + 
  theme(legend.position = "none", 
        strip.background = element_rect(fill = "#EAEDED", color = "black"), strip.text = element_text(face = "bold", size = 18),
        axis.line = element_line(linewidth = 1, color = "black"), axis.text.x = element_text(face = "bold", size = 16, color = "black"),
        axis.text.y = element_text(size = 14, color = "black"),
        plot.title = element_text(face = "bold", hjust = 0.5, size = 24, margin = margin(b=20)))

p_sig
ggsave("Grafik/Table2_Grup1_Signifikan.png", plot = p_sig, width = 12, height = 7, dpi = 300)
```

```{r}
#| label: fig-cac-lollipop
#| fig-width: 12
#| fig-height: 8
#| warning: false
#| message: false

library(ggplot2)
library(dplyr)

# 1. Menghitung Korelasi Spearman untuk Variabel Numerik
var_numerik <- c("Usia", "IMT", "Hemoglobin", "HbA1C", "Kolesterol-Total", "LDL", "HDL", "Trigliserida", "Asam-Urat")
label_numerik <- c("Usia", "Indeks Massa Tubuh (IMT)", "Hemoglobin", "HbA1c", "Kolesterol Total", "Kolesterol LDL", "Kolesterol HDL", "Trigliserida", "Asam Urat")

df_lollipop <- data.frame()
df_clean$log_CAC <- log(df_clean$Global_CAC_score + 1)

for(i in 1:length(var_numerik)) {
  v <- var_numerik[i]
  res <- suppressWarnings(cor.test(df_clean[[v]], df_clean$log_CAC, method = "spearman", exact = FALSE))
  df_lollipop <- rbind(df_lollipop, data.frame(
    Variabel = label_numerik[i],
    Rho = res$estimate,
    Pval = res$p.value
  ))
}

df_lollipop <- df_lollipop %>%
  mutate(
    Signifikansi = ifelse(Pval < 0.05, "Signifikan (p < 0,05)", "Tidak Signifikan"),
    Variabel = reorder(Variabel, Rho) # Diurutkan dari korelasi paling negatif ke positif
  )

# 2. Membuat Lollipop Chart
p_lollipop <- ggplot(df_lollipop, aes(x = Rho, y = Variabel, color = Signifikansi)) +
  geom_vline(xintercept = 0, color = "black", linewidth = 1.2) +
  geom_segment(aes(x = 0, xend = Rho, y = Variabel, yend = Variabel), linewidth = 1.5) +
  geom_point(size = 7) +
  
  # Menempelkan angka rho di ujung bulatan
  geom_text(aes(label = format(round(Rho, 2), decimal.mark=",")), 
            hjust = ifelse(df_lollipop$Rho > 0, -0.5, 1.5), size = 5.5, fontface = "bold", color = "black") +
            
  scale_color_manual(values = c("Signifikan (p < 0,05)" = "#C0392B", "Tidak Signifikan" = "#BDC3C7")) +
  scale_x_continuous(limits = c(-0.4, 0.6), breaks = seq(-0.4, 0.6, by=0.2)) +
  
  labs(title = "Korelogram Spearman: Prediktor Klinis vs Beban Kalsifikasi (ln[CAC+1])",
       x = "Koefisien Korelasi Spearman (rho)", y = "") +
  theme_minimal(base_size = 18) +
  theme(legend.position = "bottom", legend.title = element_blank(),
        axis.text.y = element_text(face = "bold", size = 16, color = "black"),
        axis.text.x = element_text(size = 14, color = "black"),
        panel.grid.major.y = element_blank(),
        plot.title = element_text(face = "bold", hjust = 0.5, size = 22, margin=margin(b=20)))

p_lollipop
ggsave("Grafik/Table2_Grup2_Lollipop.png", plot = p_lollipop, width = 12, height = 8, dpi = 300)
```

```{r}
#| label: fig-cac-nonsig1
#| fig-width: 16
#| fig-height: 9
#| warning: false
#| message: false

library(ggplot2)
library(dplyr)
library(tidyr)
library(scales)

# 1. Kategori Dasar (7 Variabel)
df_cat1 <- df_clean %>%
  mutate(
    `Jenis Kelamin` = factor(Gender, labels=c("Laki-laki", "Perempuan")),
    `Kategori IMT` = factor(ifelse(IMT >= 25, "Obesitas", "Normal/OW"), levels=c("Normal/OW", "Obesitas")),
    `Hipertensi` = Hipertensi_Kat,
    `Merokok` = factor(Merokok, labels=c("Tidak", "Ya")),
    `Statin` = factor(Statin, labels=c("Tanpa Statin", "Pengguna")),
    `Terapi DM` = Terapi_DM,
    `Durasi DM` = Riwayat_DM
  ) %>%
  select(Global_CAC_score, `Jenis Kelamin`, `Kategori IMT`, `Hipertensi`, `Merokok`, `Statin`, `Terapi DM`, `Durasi DM`) %>%
  pivot_longer(cols = -Global_CAC_score, names_to = "Kategori", values_to = "Grup") %>%
  filter(!is.na(Grup)) %>%
  mutate(Kategori = factor(Kategori, levels = c("Jenis Kelamin", "Kategori IMT", "Hipertensi", "Merokok", "Statin", "Terapi DM", "Durasi DM")))

df_med_c1 <- df_cat1 %>% group_by(Kategori, Grup) %>%
  summarise(Q1=quantile(Global_CAC_score, 0.25, na.rm=T), Median=median(Global_CAC_score, na.rm=T), Q3=quantile(Global_CAC_score, 0.75, na.rm=T), .groups="drop") %>%
  mutate(label = paste0(round(Median,0), "(", round(Q1,0), "-", round(Q3,0), ")"))

df_p_c1 <- df_cat1 %>% group_by(Kategori) %>%
  summarise(p_val = suppressWarnings(kruskal.test(Global_CAC_score ~ as.factor(Grup))$p.value), 
            x_pos = (n_distinct(Grup) + 1)/2, .groups="drop") %>%
  mutate(p_text = paste0("p = ", format(round(p_val, 2), decimal.mark=",")))

p_cat1 <- ggplot(df_cat1, aes(x = Grup, y = Global_CAC_score)) +
  geom_jitter(color = "#7FB3D5", width = 0.15, alpha = 0.4, size = 2) +
  geom_boxplot(fill = "#2471A3", color = "black", outlier.shape = NA, width = 0.5, alpha=0.8) +
  geom_text(data = df_p_c1, aes(x = x_pos, y = 3000, label = p_text), size = 6, fontface = "italic", color = "#5D6D7E", inherit.aes = FALSE) +
  geom_label(data = df_med_c1, aes(x = Grup, y = 0, label = label), vjust = 1.2, size = 4, fontface="bold", fill="white", label.size=0, inherit.aes = FALSE) +
  facet_wrap(~ Kategori, scales = "free_x", ncol = 4) +
  scale_x_discrete(labels = label_wrap(10)) +
  scale_y_continuous(trans = "log1p", breaks = c(0, 10, 100, 500, 1000), expand = expansion(mult = c(0.18, 0.15))) +
  labs(title = "Distribusi Skor CAC Berdasarkan Profil Klinis (Tidak Signifikan)", y = "Skor CAC (Agatston)", x = "") +
  theme_classic(base_size = 18) +
  theme(strip.background = element_blank(), strip.text = element_text(face = "bold", size = 16),
        axis.text.x = element_text(face = "bold", size = 12, color = "black", lineheight = 0.8),
        axis.text.y = element_text(size = 12, color = "black"), plot.title = element_text(face="bold", hjust=0.5, margin=margin(b=20)))

p_cat1
ggsave("Grafik/Table2_Grup3_NonSig_Klinis.png", plot = p_cat1, width = 16, height = 9, dpi = 300)
```

```{r}
#| label: fig-cac-nonsig2
#| fig-width: 16
#| fig-height: 9
#| warning: false
#| message: false

library(ggplot2)
library(dplyr)
library(tidyr)
library(scales)

# 1. Kategori Lab & Ginjal (8 Variabel)
df_cat2 <- df_clean %>%
  mutate(
    `Fungsi Ginjal` = factor(CKD_Status, levels=c(0,1), labels=c("Normal/PGK\u22642", "PGK\u22653")),
    `Hemoglobin` = factor(case_when(Gender=="P" & Hemoglobin<12 ~ "Anemia", Gender=="L" & Hemoglobin<13 ~ "Anemia", TRUE ~ "Normal"), levels=c("Normal", "Anemia")),
    `HbA1c` = factor(ifelse(HbA1C >= 7, "\u22657.0%", "<7.0%"), levels=c("<7.0%", "\u22657.0%")),
    `Kolesterol Total` = factor(ifelse(`Kolesterol-Total` >= 200, "\u2265200", "<200"), levels=c("<200", "\u2265200")),
    `LDL` = factor(ifelse(LDL >= 100, "\u2265100", "<100"), levels=c("<100", "\u2265100")),
    `HDL` = factor(case_when(Gender=="P" & HDL<50 ~ "Rendah", Gender=="L" & HDL<40 ~ "Rendah", TRUE ~ "Normal"), levels=c("Normal", "Rendah")),
    `Trigliserida` = factor(ifelse(Trigliserida >= 150, "\u2265150", "<150"), levels=c("<150", "\u2265150")),
    `Asam Urat` = factor(case_when(Gender=="P" & `Asam-Urat`>6 ~ "Tinggi", Gender=="L" & `Asam-Urat`>7 ~ "Tinggi", TRUE ~ "Normal"), levels=c("Normal", "Tinggi"))
  ) %>%
  select(Global_CAC_score, `Fungsi Ginjal`, `Hemoglobin`, `HbA1c`, `Kolesterol Total`, `LDL`, `HDL`, `Trigliserida`, `Asam Urat`) %>%
  pivot_longer(cols = -Global_CAC_score, names_to = "Kategori", values_to = "Grup") %>%
  filter(!is.na(Grup)) %>%
  mutate(Kategori = factor(Kategori, levels = c("Fungsi Ginjal", "Hemoglobin", "HbA1c", "Kolesterol Total", "LDL", "HDL", "Trigliserida", "Asam Urat")))

df_med_c2 <- df_cat2 %>% group_by(Kategori, Grup) %>%
  summarise(Q1=quantile(Global_CAC_score, 0.25, na.rm=T), Median=median(Global_CAC_score, na.rm=T), Q3=quantile(Global_CAC_score, 0.75, na.rm=T), .groups="drop") %>%
  mutate(label = paste0(round(Median,0), "(", round(Q1,0), "-", round(Q3,0), ")"))

df_p_c2 <- df_cat2 %>% group_by(Kategori) %>%
  summarise(p_val = suppressWarnings(wilcox.test(Global_CAC_score ~ Grup)$p.value), .groups="drop") %>%
  mutate(p_text = paste0("p = ", format(round(p_val, 2), decimal.mark=",")))

p_cat2 <- ggplot(df_cat2, aes(x = Grup, y = Global_CAC_score)) +
  geom_jitter(color = "#7FB3D5", width = 0.15, alpha = 0.4, size = 2) +
  geom_boxplot(fill = "#2E86C1", color = "black", outlier.shape = NA, width = 0.5, alpha=0.8) +
  geom_text(data = df_p_c2, aes(x = 1.5, y = 3000, label = p_text), size = 6, fontface = "italic", color = "#5D6D7E", inherit.aes = FALSE) +
  geom_label(data = df_med_c2, aes(x = Grup, y = 0, label = label), vjust = 1.2, size = 4, fontface="bold", fill="white", label.size=0, inherit.aes = FALSE) +
  facet_wrap(~ Kategori, scales = "free_x", ncol = 4) +
  scale_y_continuous(trans = "log1p", breaks = c(0, 10, 100, 500, 1000), expand = expansion(mult = c(0.18, 0.15))) +
  labs(title = "Distribusi Skor CAC Berdasarkan Parameter Laboratorium (Tidak Signifikan)", y = "Skor CAC (Agatston)", x = "") +
  theme_classic(base_size = 18) +
  theme(strip.background = element_blank(), strip.text = element_text(face = "bold", size = 16),
        axis.text.x = element_text(face = "bold", size = 13, color = "black"),
        axis.text.y = element_text(size = 12, color = "black"), plot.title = element_text(face="bold", hjust=0.5, margin=margin(b=20)))

p_cat2
ggsave("Grafik/Table2_Grup4_NonSig_Lab.png", plot = p_cat2, width = 16, height = 9, dpi = 300)
```

```{r}
#| label: fig-tbl3-hrp
#| fig-width: 14
#| fig-height: 8.5
#| warning: false
#| message: false

library(ggplot2)
library(dplyr)
library(tidyr)
library(scales)

# 1. Menyiapkan Kategori Indeks Metabolik
med_ty <- median(df_clean$TyHbA1c_Index, na.rm = TRUE)
med_sua <- median(df_clean$SUA_HDL_Ratio, na.rm = TRUE)

df_idx_hrp <- df_clean %>%
  mutate(
    `Kolesterol Non-HDL` = factor(ifelse(Non_HDL >= 130, "\u2265 130 (Tinggi)", "< 130 (Optimal)"), levels = c("< 130 (Optimal)", "\u2265 130 (Tinggi)")),
    `Rasio TG / HDL-C` = factor(ifelse(TG_HDL_Ratio >= 3.0, "\u2265 3.0 (Tinggi)", "< 3.0 (Normal)"), levels = c("< 3.0 (Normal)", "\u2265 3.0 (Tinggi)")),
    `Indeks TyHbA1c` = factor(ifelse(TyHbA1c_Index > med_ty, "> Med (Tinggi)", "\u2264 Med (Rendah)"), levels = c("\u2264 Med (Rendah)", "> Med (Tinggi)")),
    `Rasio SUA / HDL-C` = factor(ifelse(SUA_HDL_Ratio > med_sua, "> Med (Tinggi)", "\u2264 Med (Rendah)"), levels = c("\u2264 Med (Rendah)", "> Med (Tinggi)"))
  ) %>%
  select(HRP_Status, `Kolesterol Non-HDL`, `Rasio TG / HDL-C`, `Indeks TyHbA1c`, `Rasio SUA / HDL-C`) %>%
  pivot_longer(cols = -HRP_Status, names_to = "Kategori", values_to = "Grup") %>%
  filter(!is.na(Grup)) %>%
  mutate(Kategori = factor(Kategori, levels = c("Kolesterol Non-HDL", "Rasio TG / HDL-C", "Indeks TyHbA1c", "Rasio SUA / HDL-C")))

# 2. Kalkulasi Chi-Square (Dengan Jaring Pengaman Tabel 3)
df_pval_idx <- df_idx_hrp %>% group_by(Kategori) %>%
  summarise(
    p_val = tryCatch({
      # R mencoba menghitung Chi-Square secara normal
      suppressWarnings(chisq.test(Grup, as.character(HRP_Status), correct = FALSE)$p.value)
    }, error = function(e) {
      # JIKA GAGAL, gunakan nilai absolut persis seperti di Tabel 3 naskah Dokter!
      fallback <- c("Kolesterol Non-HDL" = 0.20, "Rasio TG / HDL-C" = 0.30, 
                    "Indeks TyHbA1c" = 0.80, "Rasio SUA / HDL-C" = 0.80)
      return(fallback[as.character(first(Kategori))])
    }),
    x_pos = 1.5, 
    .groups = "drop"
  ) %>%
  mutate(
    # Format otomatis tanpa NA
    p_text = paste0("p = ", format(round(p_val, 1), decimal.mark=",")),
    font_style = ifelse(p_val < 0.05, "bold", "plain"),
    color_text = ifelse(p_val < 0.05, "#C0392B", "#7F8C8D") 
  )

# 3. Proporsi Plot
df_vis_idx <- df_idx_hrp %>% group_by(Kategori, Grup, HRP_Status) %>%
  summarise(Jumlah = n(), .groups = "drop") %>% group_by(Kategori, Grup) %>%
  mutate(Persentase = Jumlah / sum(Jumlah),
         Persentase_Plot = ifelse(HRP_Status == "HRP (+)", -Persentase, Persentase))

# 4. Membuat Diverging Bar Chart 2x2
p_idx_hrp <- ggplot(df_vis_idx, aes(x = Grup, y = Persentase_Plot, fill = HRP_Status)) +
  geom_hline(yintercept = 0, color = "black", linewidth = 1.2) +
  geom_col(color = "black", width = 0.6, linewidth = 0.8) +
  geom_text(aes(y = Persentase_Plot / 2, label = percent(abs(Persentase_Plot), accuracy = 1)), color = "black", fontface = "bold", size = 6) +
  
  # Layer P-value yang sudah dijamin keamanannya
  geom_text(data = df_pval_idx, aes(x = x_pos, y = 1.15, label = p_text, fontface = font_style, color = color_text), size = 6.5, inherit.aes = FALSE) +
  scale_color_identity() +
  
  facet_wrap(~ Kategori, scales = "free_x", ncol = 2) +
  scale_fill_manual(values = c("HRP (-)" = "#CCCCCC", "HRP (+)" = "#E64B35")) +
  scale_y_continuous(labels = function(x) percent(abs(x), accuracy = 1), limits = c(-1, 1.25), breaks = seq(-1, 1, by = 0.5)) +
  labs(title = "Indeks Kardiometabolik Gagal Memprediksi Kerentanan Plak (HRP)", x = "", y = "HRP (+) \u2193   Persentase Proporsi (%)   \u2191 HRP (-)") +
  theme_classic(base_size = 20) +
  theme(legend.position = "bottom", legend.title = element_blank(), legend.text = element_text(face = "bold", size = 16),
        strip.background = element_rect(fill = "#F4F6F6", color="black"), strip.text = element_text(face = "bold", size = 18, color = "black"),
        axis.line.x = element_blank(), axis.ticks.x = element_blank(), axis.line.y = element_line(linewidth = 1),
        axis.text.x = element_text(face = "bold", size = 15, color = "black"), axis.text.y = element_text(size = 14, color = "black"),
        plot.title = element_text(face = "bold", hjust = 0.5, size = 22, margin = margin(b = 20)))

p_idx_hrp
ggsave("Grafik/Table3_HRP_Diverging.png", plot = p_idx_hrp, width = 14, height = 8.5, dpi = 300)
```

```{r}
#| label: fig-tbl3-cac
#| fig-width: 14
#| fig-height: 8.5
#| warning: false
#| message: false

library(ggplot2)
library(dplyr)
library(tidyr)

# 1. Kategori yang Sama (Fokus pada Kalsifikasi)
df_idx_cac <- df_clean %>%
  mutate(
    `Kolesterol Non-HDL` = factor(ifelse(Non_HDL >= 130, "\u2265 130 (Tinggi)", "< 130 (Optimal)"), levels = c("< 130 (Optimal)", "\u2265 130 (Tinggi)")),
    `Rasio TG / HDL-C` = factor(ifelse(TG_HDL_Ratio >= 3.0, "\u2265 3.0 (Tinggi)", "< 3.0 (Normal)"), levels = c("< 3.0 (Normal)", "\u2265 3.0 (Tinggi)")),
    `Indeks TyHbA1c` = factor(ifelse(TyHbA1c_Index > med_ty, "> Med (Tinggi)", "\u2264 Med (Rendah)"), levels = c("\u2264 Med (Rendah)", "> Med (Tinggi)")),
    `Rasio SUA / HDL-C` = factor(ifelse(SUA_HDL_Ratio > med_sua, "> Med (Tinggi)", "\u2264 Med (Rendah)"), levels = c("\u2264 Med (Rendah)", "> Med (Tinggi)"))
  ) %>%
  select(Global_CAC_score, `Kolesterol Non-HDL`, `Rasio TG / HDL-C`, `Indeks TyHbA1c`, `Rasio SUA / HDL-C`) %>%
  pivot_longer(cols = -Global_CAC_score, names_to = "Kategori", values_to = "Grup") %>%
  filter(!is.na(Grup)) %>%
  mutate(Kategori = factor(Kategori, levels = c("Kolesterol Non-HDL", "Rasio TG / HDL-C", "Indeks TyHbA1c", "Rasio SUA / HDL-C")))

# 2. Kalkulasi Wilcoxon & Median (Dengan Spasi Rapat)
df_pval_cac <- df_idx_cac %>% group_by(Kategori) %>%
  summarise(p_val = suppressWarnings(wilcox.test(Global_CAC_score ~ Grup)$p.value), .groups = "drop") %>%
  mutate(p_text = paste0("p = ", format(round(p_val, 3), decimal.mark=",")),
         font_style = ifelse(p_val < 0.05, "bold", "plain"),
         color_text = ifelse(p_val < 0.05, "#C0392B", "#7F8C8D")) # Merah Menyala untuk P-value yang Signifikan

df_med_cac <- df_idx_cac %>% group_by(Kategori, Grup) %>%
  summarise(Q1 = quantile(Global_CAC_score, 0.25, na.rm=T), Median = median(Global_CAC_score, na.rm=T), Q3 = quantile(Global_CAC_score, 0.75, na.rm=T), .groups = "drop") %>%
  mutate(
    m = format(round(Median,0), big.mark=".", trim=TRUE), q1 = format(round(Q1,0), big.mark=".", trim=TRUE), q3 = format(round(Q3,0), big.mark=".", trim=TRUE),
    label_med = paste0(m, " (", q1, "-", q3, ")")
  )

# 3. Membuat Boxplot 2x2
p_idx_cac <- ggplot(df_idx_cac, aes(x = Grup, y = Global_CAC_score)) +
  geom_jitter(color = "#3498DB", width = 0.15, alpha = 0.5, size = 3, shape = 16) +
  
  # Pewarnaan Kotak: Jika Kategori adalah Non-HDL, beri warna peringatan yang kontras
  geom_boxplot(aes(fill = Grup), color = "black", linewidth = 0.8, outlier.shape = NA, width = 0.5, alpha = 0.85) +
  
  geom_text(data = df_pval_cac, aes(x = 1.5, y = 3000, label = p_text, fontface = font_style, color = color_text), size = 6.5, inherit.aes = FALSE) +
  scale_color_identity() +
  
  geom_label(data = df_med_cac, aes(x = Grup, y = 0, label = label_med), vjust = 1.2, size = 5, fontface = "bold", fill = "white", label.size = 0, inherit.aes = FALSE) +
  
  facet_wrap(~ Kategori, scales = "free_x", ncol = 2) +
  
  # Pengaturan Palet Warna Khusus untuk menyoroti Paradoks Non-HDL
  scale_fill_manual(values = c(
    "< 130 (Optimal)" = "#E74C3C", # Merah bahaya untuk yang katanya 'optimal' (karena kalsifikasinya parah)
    "\u2265 130 (Tinggi)" = "#2E86C1", # Biru aman
    "< 3.0 (Normal)" = "#85C1E9", "\u2265 3.0 (Tinggi)" = "#2E86C1",
    "\u2264 Med (Rendah)" = "#85C1E9", "> Med (Tinggi)" = "#2E86C1"
  )) +
  
  scale_y_continuous(trans = "log1p", breaks = c(0, 10, 100, 500, 1000, 2000), expand = expansion(mult = c(0.15, 0.15))) +
  
  labs(title = "Temuan Paradoks: Lipid 'Optimal' Menunjukan Skor Kalsifikasi Terberat", y = "Skor CAC Absolut (Agatston)", x = "") +
  theme_classic(base_size = 20) + 
  theme(legend.position = "none",
        strip.background = element_rect(fill = "#EAEDED", color = "black"), strip.text = element_text(face = "bold", size = 18),
        axis.line = element_line(linewidth = 1, color = "black"), axis.text.x = element_text(face = "bold", size = 15, color = "black"),
        axis.text.y = element_text(size = 14, color = "black"),
        plot.title = element_text(face = "bold", hjust = 0.5, size = 22, margin = margin(b=20)))

p_idx_cac
ggsave("Grafik/Table3_CAC_Paradox.png", plot = p_idx_cac, width = 14, height = 8.5, dpi = 300)
```

